# تاریخچهٔ نسخه‌ها

نسخه در `manifest.json` نگه داشته می‌شود و در «تنظیمات» پایین صفحه دیده می‌شود.
قاعده: هر بستهٔ تغییر، یک شمارهٔ جدید.

---

## ۱٫۱٫۶ — سه خبر همزمان

کارت اخبار یک خبرِ عریض نشان می‌داد و هر ۲۰ ثانیه عوضش می‌کرد. حالا **سه خبر
کنارِ هم** می‌آیند و دکمه‌های ورق‌زدن سه‌تا سه‌تا جلو می‌روند.

**چرا سه و نه چهار؟** در عرضِ کارت هر ستون حدود ۳۰۰ پیکسل می‌شود و تیترِ فارسی
در آن جا می‌گیرد. با چهار ستون هر ستون ~۲۲۵ پیکسل می‌شد، تیترها بد می‌شکستند و
جای خلاصه نمی‌ماند.

در ستونِ باریک **تیتر زیرِ عکس می‌آید، نه رویش** — روی عکسِ کوچک خوانا نبود.
هر کاشی خودش لینک است، پس هدفِ کلیک بزرگ‌تر شد.

**چرخش کندتر شد** — از ۲۰ ثانیه به ۴۵. وقتی سه خبر همزمان دیده می‌شود، عوض شدنِ
مدامشان فقط حواس‌پرتی است.

کارت روی صفحهٔ باریک خودش جمع می‌شود: سه ستون، بعد دو، بعد یکی.

عنوان کارت از «اخبار فناوری» به **«اخبار»** تغییر کرد؛ با اضافه شدنِ منابعِ
ورزشی و سینمایی و اقتصادی دیگر درست نبود.

---

## ۱٫۱٫۵ — شش منبع خبر و دستهٔ «سینما»

شش منبع به فهرست اضافه شد:

| منبع | دسته |
|---|---|
| پیوست | فناوری |
| راه پرداخت | فناوری |
| تجارت‌نیوز | اقتصاد |
| اکوایران | اقتصاد |
| فوتبالی | ورزشی |
| فیلم‌نیوز | سینما |

**دستهٔ «سینما» تازه است.** بدون آن فیلم‌نیوز می‌افتاد در «عمومی» و کنارِ خبرِ
سیاسی و اقتصادی گم می‌شد.

فهرست دسته‌ها دو جا نوشته شده بود — یکی در `core/kiosk.js` برای سنجیدنِ فیدهای
دلخواه و یکی در `app.js` برای انتخابگرِ منابع. حالا هر دو از یک جا می‌خوانند،
وگرنه دستهٔ تازه در یکی از دو فهرست جا می‌ماند.

> **آدرسِ این فیدها راستی‌آزمایی نشده.** اگر منبعی جواب نداد، در تنظیمات از
> انتخاب درش بیاور و اگر آدرسِ درستش را می‌دانی Issue باز کن.

---

## ۱٫۱٫۴ — خلاصهٔ خبر زیر تیتر

کارت اخبار حالا **چند خط از خودِ خبر** را هم نشان می‌دهد، نه فقط تیتر. متن از
`description` همان فید می‌آید — که تا حالا فقط برای پیدا کردنِ عکس خوانده و بعد
دور ریخته می‌شد.

**کوتاه می‌ماند.** سقفِ ۲۴۰ نویسه، و در کارت هم سه خط بیشتر نه. هدف این است که
بفهمی خبر دربارهٔ چیست، نه اینکه کلِ خبر را اینجا بخوانی.

**متنِ فید تمیز می‌شود:**

- تگ‌های HTML و انتیتی‌ها (`&amp;` و مانندش) به متنِ ساده تبدیل می‌شوند
- پانویسی که وردپرس ته هر خبر می‌گذارد («نوشته … اولین بار در … پدیدار شد») حذف
  می‌شود؛ هیچ‌چیز به خواننده نمی‌گفت
- «ادامه مطلب…» ته متن برداشته می‌شود
- اگر فیدی توضیح را همان تیتر گذاشته باشد، تکرارش زیر تیتر نمی‌آید

**دستهٔ خبر هم دیده می‌شود** — «فناوری»، «ورزشی»، «اقتصاد»، «عمومی». این را از
اول داشتیم، فقط جایی نشان داده نمی‌شد.

خبری که توضیح ندارد جای خالی نمی‌گذارد؛ کارت همان شکلِ قبلی را دارد.

۹ تست تازه برای همین پاک‌سازی (۵۳۵ تست).

---

## ۱٫۱٫۳ — کارت اخبار با عکس

کارت «اخبار فناوری» در صفحهٔ امروز یک خطِ متن بود. حالا **عکسِ خودِ خبر** را نشان
می‌دهد و تیتر روی آن می‌نشیند.

نکتهٔ جالب: عکس‌ها از قبل استخراج و پاک‌سازی می‌شدند (فقط `https`، فقط چیزی که
واقعاً عکس باشد) — فقط هیچ‌وقت نمایش داده نمی‌شدند.

**قدِ هیرو از نسبتِ تصویر نمی‌آید.** اگر می‌آمد، هر خبر با عکسی به نسبتِ دیگر
صفحهٔ «امروز» را بالا و پایین می‌پراند. قد با عرضِ صفحه بزرگ می‌شود ولی سقف
دارد، و سقفِ دومی روی بلندیِ پنجره هست تا در نمایشگرِ کوتاه کلِ صفحه را نخورد.

**هیچ خبری بدونِ عکس زشت نمی‌شود.** اگر عکسی نبود — یا بود و بارگذاری نشد، که
در فیدها زیاد پیش می‌آید — زمینهٔ گرادیانی با نامِ منبع می‌آید. قابِ خالی بدتر
از نبودِ عکس است.

**حریم خصوصی:** عکس‌ها با `referrerpolicy="no-referrer"` بارگذاری می‌شوند تا
میزبانِ عکس نفهمد از کجا آمده‌ای. عکسِ خبرِ بعدی هم از قبل گرفته می‌شود تا
چرخشِ هر ۲۰ ثانیه پرش نداشته باشد، و خودِ چرخش یک محوشدگیِ کوتاه دارد
(با `prefers-reduced-motion` خاموش می‌شود).

---

## ۱٫۱٫۲ — «بررسی نسخهٔ تازه» دیگر بن‌بست نیست

وقتی دسترسی به گیت‌هاب داده نشده بود، «بررسی نسخهٔ تازه» فقط می‌گفت «دسترسی به
گیت‌هاب داده نشده» و تمام. تنها جایی که این اجازه گرفته می‌شد، دکمهٔ «دادن دسترسی
به سایت‌ها» در بخشِ اخبار بود — و ربطش به «نسخهٔ تازه» به ذهنِ کسی نمی‌رسید.

حالا **راهِ حل کنارِ خودِ پیام است**: دکمهٔ «اجازه بده» همان‌جا می‌آید و پس از
گرفتنِ اجازه، بررسی خودش دوباره اجرا می‌شود. دسترسی هم فقط برای
`api.github.com` خواسته می‌شود، نه کلِ سایت‌های خبری.

بر خلافِ بقیهٔ پیام‌ها، این یکی بعد از چند ثانیه پاک نمی‌شود — دکمه‌ای که وسطِ
خواندن ناپدید شود بدتر از نبودنش است.

**تیکِ «خبرم کن اگر نسخهٔ تازه‌ای منتشر شد» هم همین مشکل را داشت** — و بدتر، چون
هیچ‌کس منتظرِ جواب نبود. تیک پیش‌فرض روشن است و وعده می‌دهد روزی یک بار بررسی
می‌کند، ولی بدونِ اجازهٔ گیت‌هاب آن بررسی بی‌سروصدا هیچ‌وقت انجام نمی‌شد. حالا
کنارِ خودِ تیک گفته می‌شود که چه چیزی کم است، با همان دکمهٔ «اجازه بده». وقتی
تیک خاموش است چیزی نشان داده نمی‌شود — دربارهٔ قابلیتی که خودت خاموشش کرده‌ای
نباید چیزی طلب شود.

**ردیفِ نسخه هم مرتب شد.** نسخه و دکمه و پیام همه در یک خطِ وسط‌چین جمع می‌شدند و
پیامِ بلند وسطِ عبارت می‌شکست («گیت‌هاب دسترسی / ندارد — اجازه بده»). حالا نسخه و
دکمه کنارِ هم می‌مانند و پیام همیشه خطِ خودش را دارد.

---

## ۱٫۱٫۱ — جای کنترلِ ضبط روی Meet

کنترلِ شناورِ «شروع ثبت» گوشهٔ بالا-راست بود و آنجا دو مشکل داشت: هم زیرِ کنترل‌های
خودِ Meet می‌رفت، هم با باز شدنِ پنلِ کناری پوشانده می‌شد. هیچ گوشه‌ای برای همهٔ
چیدمان‌ها درست نیست، پس **جایش را خودِ کاربر تعیین می‌کند**.

- پیش‌فرض به پایین-چپ رفت — نوارِ کنترلِ Meet وسط و پایین است و پنل‌هایش سمت راست.
- یک **دستگیره** کنارِ دکمه آمد؛ با کشیدنِ آن دکمه جابه‌جا می‌شود و جایش یادش می‌ماند.
  خودِ دکمه کشیده نمی‌شود تا کلیک هیچ‌وقت با کشیدن اشتباه نشود.
- با **کلیدهای جهت‌دار** هم جابه‌جا می‌شود (با Shift گام‌های بلندتر) — دکمه نباید فقط
  با ماوس قابل‌استفاده باشد.
- تا وقتی ضبط فعال نیست کم‌رنگ می‌ماند تا جلسه را شلوغ نکند.

**کوچک‌کردنِ پنجره دیگر انتخابِ کاربر را پاک نمی‌کند.** دکمه برای اینکه بیرونِ کادر
نیفتد جمع می‌شود، ولی جایی که کاربر انتخاب کرده دست‌نخورده می‌ماند و با برگشتنِ فضا
دکمه به همان‌جا برمی‌گردد.

**نوشتنِ جای دکمه در حافظه جمع می‌شود** — نگه‌داشتنِ کلیدِ جهت‌دار وگرنه ده‌ها نوشتنِ
پشت‌سرهم می‌ساخت. بستنِ ناگهانیِ تب هم جای تازه را از دست نمی‌دهد.

۶ تست برای همین رفتار اضافه شد (۵۲۶ تست).

---

## ۱٫۱٫۰ — پل هوش مصنوعی: بستهٔ زمینه و فایل snapshot

تا حالا منشی از هوش مصنوعی می‌پرسید. حالا **هوش مصنوعیِ خودتان می‌تواند دادهٔ منشی را بخواند.**
دلیلش ساده است: هر قابلیتی که مدل بتواند بدهد، تا امروز باید رابطش در منشی نوشته می‌شد.
با این تغییر، رابط را کاربر از قبل دارد — منشی فقط منبعِ داده می‌شود.

**بپرس از هوش مصنوعی — بدون هیچ نصبی، برای همه.**
دکمه‌ای در «جلسه‌ها» و در تنظیمات. دادهٔ منشی را در کلیپ‌بورد می‌گذارد تا در ChatGPT،
Claude، جمینای یا مدل محلی بچسبانید. هیچ درخواست اینترنتی‌ای نمی‌رود.

طراحی عمداً **کارمحور** است، نه پیکربندی‌محور. کاربر «بازه» و «سطح جزئیات» انتخاب
نمی‌کند — کارش را انتخاب می‌کند:

- صورت‌جلسه‌های عقب‌افتاده را بساز
- گزارش این هفته را بنویس
- کارهایم را اولویت‌بندی کن
- برای جلسه با یک نفر آماده‌ام کن
- دربارهٔ یک جلسه بپرس
- سؤال خودم را می‌نویسم ← تنظیم دستی اینجا باز می‌شود

**سؤال داخل خودِ بسته نوشته می‌شود.** کاربر می‌چسباند و Enter می‌زند؛ لازم نیست چیزی
تایپ کند. درخواست ته متن می‌آید (نه سرش) چون در متنِ بلند، دستوری که بعد از داده بیاید
بهتر دنبال می‌شود — و یک جملهٔ قاب هم سر متن می‌آید تا کاربر بداند چه چسبانده.

**بعد از کپی، کاربر تنها نمی‌ماند.** پنجره به گام دوم می‌رود: لینکِ ChatGPT و Claude و
Gemini، میان‌برِ چسباندن، و یادآوری اینکه سؤال از قبل نوشته شده.

**اندازه به زبان آدمیزاد.** به‌جای «حدود ۱۲٬۰۰۰ توکن» می‌گوید «کوچک — در هر چت‌باتی
جا می‌شود» یا «بزرگ — ممکن است در نسخه‌های رایگان جا نشود». عددِ توکن برای کسی که
مدل را نمی‌شناسد تصمیم‌پذیر نیست.

**کنارِ هر کار، شمارشش دیده می‌شود** («۲ جلسه و ۱۰ کار» / «چیزی نیست») و پنجره روی
کاری باز می‌شود که واقعاً داده دارد — نه لزوماً اولی. کاربر نباید انتخاب کند و بعد
بفهمد خالی است.

**جلسه‌های بدون صورت‌جلسه هم کار می‌کنند** — و یکی از همان کارهای آماده دقیقاً برای
همان‌هاست. متن خام کامل‌تر از خلاصه است؛ مدل خودش می‌خواند و صورت‌جلسه می‌سازد، با
اشتراکی که از قبل دارید نه با اعتبار API.

**فایل snapshot — برای پلِ MCP.**
یک پوشه انتخاب می‌کنید و منشی `snapshot.json` را آنجا می‌نویسد (پیش‌فرض خاموش). سرور MCP
همان فایل را می‌خواند تا Claude Desktop، Claude Code، Codex یا Cursor بتوانند دادهٔ شما را
کوئری کنند. **هیچ پورتی باز نمی‌شود و هیچ چیزی روی شبکه نمی‌رود.**
فایل با هر تغییرِ داده تازه می‌شود و «آخرین نوشتن» در تنظیمات دیده می‌شود.

**سه سطح حساسیت، چون متن خام حساس‌ترین دادهٔ منشی است:**

| سطح | شامل |
|---|---|
| فقط فراداده | عنوان، تاریخ، شرکت‌کنندگان |
| صورت‌جلسه (پیش‌فرض) | + خلاصه و کارها، **بدون متن خام** |
| متن کامل | + متنِ کلمه‌به‌کلمه — با هشدار |

سطحِ نامعتبر همیشه به «صورت‌جلسه» برمی‌گردد، نه به «متن کامل» — پیش‌فرضِ امن.

**خطِ قرمزِ طراحی:** خروجی **هیچ تنظیماتی** ندارد. نه آدرس iCal، نه کلید هوش مصنوعی.
به‌جای فیلترکردنِ کلیدهای حساس — که با اضافه‌شدنِ کلیدِ تازه بی‌صدا می‌شکند — اصلاً به
`settings` دست نمی‌زنیم. یک تست هم همین را می‌سنجد: کلیدِ ساختگی را عمداً پاس می‌دهد و
مطمئن می‌شود در خروجی نیست.

**راهِ برگشت — صورت‌جلسه از بیرون به منشی می‌آید.**
تا حالا پل یک‌طرفه بود. حالا سرور MCP می‌تواند صورت‌جلسه‌ای که ساخته در `inbox.json`
بگذارد و منشی نواری نشان دهد: «۳ صورت‌جلسه رسیده — بررسی کن».

کاربردِ اصلی‌اش همان عقب‌ماندگیِ صورت‌جلسه‌هاست: در Claude Desktop یا Codex می‌گویی
«جلسه‌های بدون صورت‌جلسه را صورت‌جلسه کن»، مدل یکی‌یکی متن خام را می‌خواند و برمی‌گرداند
— با اشتراکِ ماهانه‌ات، نه با اعتبار API. دسته‌دسته، چون پنجاه جلسه حدود یک میلیون توکن است.

**سه قاعده که رویشان کوتاه نیامدم:**

- **هیچ‌چیز خودکار اعمال نمی‌شود.** محتوایی که بیرون از منشی و توسط یک مدل ساخته شده
  نباید بی‌سروصدا وارد بایگانیِ جلساتت شود. صفحهٔ بازبینی، تیک‌به‌تیک.
- **جلسه‌ای که از قبل صورت‌جلسه دارد «تعارض» است** و پیش‌فرض تیک نمی‌خورد.
- **`inbox.json` ورودیِ نامعتمد است** — هر برنامه‌ای روی دستگاه می‌تواند در آن پوشه
  بنویسد. سقف طول، بررسی نوع، ردکردنِ `kind` ناشناخته، و دورریختنِ شناسهٔ جلسهٔ ناموجود.

**سرور MCP** در `mcp/manshi-mcp.js` — بدون هیچ وابستگی، فقط Node. پنج ابزار:
`list_meetings`، `get_meeting`، `list_tasks`، `write_minutes`، `inbox_status`.
هر جواب `_dataFrom` دارد تا مدل بداند دادهٔ فایل تا چه تاریخی است.
راه‌اندازی در [mcp/README.md](mcp/README.md). با `--read-only` نوشتن خاموش می‌شود.

**خاموش‌کردنِ پل، فایل را پاک نمی‌کرد.** این حفره را کاربر پیدا کرد: تیک را که
برمی‌داشتی نوشتن متوقف می‌شد ولی `snapshot.json` با تمام محتوایش سرِ جایش می‌ماند —
بیرون از جعبهٔ شنیِ کروم، جایی که هر برنامه‌ای روی دستگاه می‌تواند بخواندش. حالا تا
وقتی فایلی مانده باشد، در تنظیمات پنلی می‌ماند که نامشان و حجمشان را می‌گوید و یک
دکمهٔ پاک‌کردن دارد. پنجرهٔ تأیید نگذاشتم چون کاربر ردش می‌کند و دیگر نمی‌بیندش.
ضمناً پلِ خاموش دیگر صندوق ورودی را هم نمی‌خواند — خاموش یعنی خاموش.

**باگ (گزارشِ کاربر): با برداشتنِ تیک، پنلِ پاک‌کردن نمی‌آمد.** علتش شکستِ خاموش بود —
پس از بازکردن دوبارهٔ مرورگر، اجازهٔ پوشه به «prompt» برمی‌گردد و بدون کلیکِ کاربر
برنمی‌گردد؛ کدِ من هم خطا را می‌بلعید و پنل را پنهان می‌کرد. یعنی کاربر فکر می‌کرد
فایلی نمانده در حالی که مانده بود — بدترین حالت برای چیزی که کارش هشدار دادن است.

حالا اگر پوشه‌ای انتخاب شده باشد، پنل **همیشه** می‌آید؛ اگر اجازه برنگشته باشد
می‌گوید یک بار اجازه لازم است و خودِ دکمه می‌گیردش.

سرِ راه یک نگرانیِ درست هم در پنل جواب داده شد: **پاک‌کردن این فایل‌ها به جلسه‌ها و
کارها دست نمی‌زند.** آن‌ها کپی‌اند؛ اصلِ داده در `chrome.storage.local` می‌ماند.

### جلسه ← پوشه، و فهرستِ مستقلِ پوشه‌ها

دو نقطهٔ کور که کاربر گرفتشان:

**۱. جلسه‌ای که کار نداشت، در هیچ پوشه‌ای نبود.** ارتباط فقط مشتق بود (از `meetingRef`ِ
کارها)، پس جلسهٔ بدونِ کار جایی نمی‌نشست و نمی‌شد گفت «این جلسه برای فلان مشتری است».

حالا جلسه `projectId` اختیاری دارد و پروندهٔ پروژه **اجتماعِ هر دو** را نشان می‌دهد:
جلسه‌هایی که کاری از آن‌ها به این پروژه وصل است، **به‌علاوهٔ** جلسه‌هایی که صریح به
آن نسبت داده شده‌اند. اشتقاق حذف نشد — به‌عنوان حدسِ پیش‌فرض ماند.

در صفحهٔ جلسه یک چیپ آمد. اگر نسبت **مشتق** باشد خط‌چین است و می‌گوید «از روی کارهای
این جلسه حدس زده شده»؛ با یک کلیک قطعی می‌شود. اگر چند کار روی چند پروژه پخش باشند،
پرتکرارترین برنده است.

**۲. پوشه‌ها فهرستِ مستقل نداشتند** — فقط از راه چیپِ روی یک کار پیدا می‌شدند، یعنی
پوشهٔ خالی عملاً نامرئی بود. حالا بالای «کارها» نوارِ **پوشه‌ها** هست: هر پوشه با
شمارِ کارِ باز، مرحله‌اش (اگر «در جریان» نباشد) و نامِ زیرپوشه‌هایش. دکمهٔ «پوشهٔ تازه»
هم همان‌جاست.

**و حذفِ پوشه حالا نسبتِ جلسه‌ها را هم پاک می‌کند** — وگرنه جلسه به پوشه‌ای اشاره
می‌ماند که وجود ندارد. خودِ جلسه‌ها البته دست‌نخورده می‌مانند.

### پروندهٔ پروژه

پروژه تا حالا فقط یک برچسبِ گروه‌بندی بود. حالا **یک مکان** است: چیپِ پروژه روی هر
کار، پرونده‌اش را باز می‌کند.

**کارِ بعدی، نه فهرستِ کارها.** بالای پرونده یک چیز است: بعدی چیست و چرا. از همان
`taskScore`ِ موجود می‌آید و دلیلش را هم می‌گوید («۵ روز عقب‌افتاده»). کارِ سپرده‌شده
هرگز کارِ بعدیِ تو نیست.

**همه‌چیز مشتق است.** جلسه‌ها از `meetingRef`ِ کارها، آدم‌ها از گویندگانِ همان جلسه‌ها
به‌علاوهٔ مسئولِ کارها، شمارش‌ها از خودِ کارها. **هیچ فیلدِ تازه‌ای جز `stage` اضافه
نشد** — پرونده با جابه‌جایی کارها خودش درست می‌ماند.

جلسه‌ها کلیک‌پذیرند و مستقیم به همان جلسه می‌روند؛ آن‌هایی که صورت‌جلسه ندارند
علامت می‌خورند. آدمی که بیش از دو هفته دیده نشده، خط‌چین می‌شود.

**مرحله**: ایده / در جریان / منتظر / تمام‌شده. عمداً عمومی، نه فروش‌محور — منشی CRM
نیست و قرار نیست بشود.

### توضیحات زمان‌دار

**باگ (گزارشِ کاربر): «افزودن توضیح» بارِ دوم کار نمی‌کرد.** دو علت داشت و هر دو در
یک تابع بودند: شنوندهٔ «کلیک بیرون» با `{ once: true }` ثبت می‌شد، پس **حتی وقتی
کلیک داخلِ پاپ‌آپ بود خودش را برمی‌داشت** و بعد پاپ‌آپ با کلیکِ بیرون بسته نمی‌شد؛
و `closePops()` هیچ‌وقت شنونده را پاک نمی‌کرد، پس شنونده‌ها تلنبار می‌شدند و کلیکِ
بعدی را می‌بلعیدند. حالا هر دو مدیریت می‌شوند — چهار چرخهٔ پشت‌سرهم آزموده شد.

**و توضیحات دفترچه شد.** هر توضیح یک ثبتِ جدا با زمانِ خودش است؛ توضیحِ تازه رویش
اضافه می‌شود، نه اینکه قبلی را پاک کند. تازه‌ترین بالا، با زمانِ نسبی («همین الان»،
«دیروز ۱۶:۳۰»). هر ثبت جدا حذف می‌شود و ⌘/Ctrl+Enter هم ثبت می‌کند.

`notes` (رشته) دست‌نخورده ماند چون هفت جای دیگر مصرفش می‌کنند — آخرین ثبت در آن
می‌نشیند. کارهای قدیمی **بدون بازنویسیِ داده** خوانده می‌شوند: توضیحِ قدیمی به یک
ثبت تبدیل می‌شود و با اولین توضیحِ تازه وارد دفترچه می‌شود تا گم نشود.

یک تصمیم که تستِ موجود گرفتش: در تکرارِ کار، **متنِ توضیح ارث می‌رود ولی دفترچه نه**
— آن سابقهٔ رخدادِ قبلی است، نه دستورالعملِ کار.

### پروژه و حوزه

برچسب برای «چند دستهٔ هم‌زمان» خوب است، ولی سازماندهی سلسله‌مراتب می‌خواهد. حالا
کارها **یک پروژه** دارند (و همچنان چند برچسب).

**یک نوع، نه دو نوع.** «حوزه» همان پروژه‌ای است که پروژه‌های دیگر زیرش‌اند —
`parentId` اختیاری، عمق حداکثر دو. بیشتر از این، خودِ سازماندهی تبدیل به کار می‌شود.
سه محافظ گذاشته شده: پروژه نمی‌تواند والدِ خودش شود، زیرِ یک زیرپروژه نمی‌رود، و
پروژه‌ای که فرزند دارد خودش فرزندِ کسی نمی‌شود.

**ارتباط با جلسه‌ها مشتق است، نه ذخیره‌شده.** جلسه‌های یک پروژه از روی `meetingRef`ِ
کارهایش درمی‌آیند (`projectMeetingRefs`). یعنی هیچ دادهٔ تازه‌ای برای همگام‌نگه‌داشتن
ساخته نشد و با جابه‌جایی کارها خودش درست می‌ماند — همان اصلی که در کلِ منشی هست:
جلسه ← کار ← آدم یک حلقه است، نه چند جدولِ جدا.

**حذفِ پروژه هیچ‌چیز را نابود نمی‌کند.** کارها بی‌پروژه می‌شوند (نه حذف)،
زیرپروژه‌ها یک سطح بالا می‌آیند (نه یتیم)، و `meetingRef` دست‌نخورده می‌ماند.

گروه‌بندیِ تازه «پروژه» کنار تاریخ/برچسب/جلسه/فرد آمد و حوزه را بالای زیرپروژه‌هایش
نشان می‌دهد. چیپِ پروژه روی کار، نامِ حوزه را هم می‌آورد («دیجی‌کالا › فاز دو») چون
«فاز دو» به‌تنهایی معنا ندارد. از داخل همان انتخابگر می‌شود پروژهٔ تازه ساخت.

**دو باگ که همین‌جا پیدا شد:**

۱. **گروه‌بندیِ پروژه کارها را ناپدید می‌کرد.** اگر پروژهٔ یک کار در درخت نبود،
کارش بی‌صدا از فهرست می‌افتاد — شبیهِ ازدست‌رفتنِ داده. حالا هر کارِ بی‌خانمان به
«بدون پروژه» می‌رود. **هیچ گروه‌بندی‌ای حق ندارد کاری را از فهرست بیندازد.**

۲. **`renderTodoAsync` پروژه‌ها را تازه نمی‌کرد** — فقط کارها را. پس پروژهٔ
تازه‌ساخته در درخت نبود و باگِ بالا را فعال می‌کرد. علتِ ریشه‌ای هم رفع شد.

### کارها: برابریِ کانبان و اولویت

**زیرکارها در کانبان اصلاً دیده نمی‌شدند.** کارت فقط عنوان و برچسب داشت — نه زیرکار،
نه مسئول، نه تخمین، نه تکرار. حالا کارت همان چیزهایی را نشان می‌دهد که ردیفِ فهرست
نشان می‌دهد، و زیرکارها یک نوارِ فشرده دارند (`۲/۳` با نوارِ پیشرفت) که با کلیک
باز می‌شود.

نکتهٔ ظریفش این است که **همان `expandedSubs`ِ فهرست را به کار می‌برد** — پس اگر
زیرکارهای یک کار را در برد باز کنی و به فهرست بروی، همان‌جا هم باز است. یک حالت،
نه دوتا. تیک‌زدنِ زیرکار هم مستقیم از داخل کارت کار می‌کند.

پنج دکمه هم به پایینِ کارت آمد: اولویت، سنجاق، زیرکار، توضیحات، حذف.

**اولویت.** چهار حالت (بدون / کم / متوسط / زیاد) که در **همان امتیازِ هوشمندِ
موجود** می‌نشیند، نه کنارش:

| | امتیاز |
|---|---|
| سنجاق‌شده | ۱۰۰۰ |
| اولویت زیاد | ۳۰۰ |
| ۵ روز عقب‌افتاده | ۲۵۰ |
| ددلاین امروز | ۱۵۰ |

یعنی «اولویت زیاد» از ددلاینِ امروز بالاتر می‌رود ولی کارِ خیلی عقب‌افتاده را زیر
خاک نمی‌کند. **اولویت جای ددلاین را نمی‌گیرد.** توضیحِ مرتب‌سازی هم اولویت را نام
می‌برد، ولی اگر کاری هم عقب‌افتاده باشد هم اولویت‌دار، عقب‌افتادگی را می‌گوید چون
فوری‌تر است. اولویت هم مثل بقیه با تکرارِ کار به نمونهٔ بعدی می‌رود.

رنگ تنها نشانه نیست: هر اولویت نقطه، رنگ و **متن** دارد.

**باگِ سرِ راه: `recurLabel` برای دادهٔ خراب متنِ بی‌معنی می‌ساخت.** هر `freq`ِ
ناشناخته — از پشتیبانِ قدیمی یا فایلِ واردشده — به شاخهٔ ماهانه می‌افتاد و
«undefined هر undefined ماه» روی چیپ می‌نشست. حالا رشتهٔ تهی برمی‌گرداند.

### توضیحات برای کارها و زیرکارها

**توضیحِ کار از قبل ذخیره می‌شد، ولی فقط در کانبان دیده می‌شد.** در نمای فهرست —
که جای اصلیِ کار کردن است — نه راهی برای نوشتنش بود نه دیده می‌شد. حالا در منوی
هر کار «افزودن توضیح» هست و متنش زیرِ عنوان می‌آید (دو خط، با کلیک باز می‌شود).

**زیرکارها اصلاً توضیح نداشتند** — شکلشان فقط `{id, title, done}` بود. حالا
`note` هم دارند: کنارِ هر زیرکار دکمه‌ای هست که پنجرهٔ کوتاهی باز می‌کند، و اگر
توضیحی داشته باشد همان‌جا زیرِ عنوانش دیده می‌شود. دکمه وقتی توضیح دارد پررنگ
می‌ماند تا از روی فهرست معلوم باشد کدام زیرکار توضیح دارد.

**نکتهٔ ریزی که مهم بود:** در تکرارِ کارها، توضیحِ زیرکار **می‌ماند** ولی تیک‌ها
صفر می‌شوند. «چطور انجامش بدهم» با تکرار عوض نمی‌شود؛ «انجام شد» می‌شود.

`updateSubtask` فقط عنوان و توضیح را دست می‌زند — نه `done`، نه `id`. عنوانِ خالی
هم زیرکار را بی‌نام نمی‌کند. توضیح سقفِ ۲۰۰۰ کاراکتری دارد تا یک چسباندنِ بی‌حواس
ذخیرهٔ اکستنشن را باد نکند.

### بازرسیِ کامل (QC)

**تیک‌های کار در RTL نیمه‌کور بودند.** ناحیهٔ کلیکِ گسترده با `inset` منطقی و
`translate(50%)` ساخته شده بود؛ در راست‌به‌چپ این ترکیب ناحیه را از مرکز بیرون
می‌برد، پس یک طرفِ چک‌باکس کلیک را نمی‌گرفت. ضمناً ناحیه فقط روی `pointer: coarse`
فعال بود، یعنی با ماوس ۲۱×۲۱ می‌ماند — **زیرِ حداقلِ ۲۴×۲۴ استانداردِ WCAG 2.5.8**.
حالا با مختصات فیزیکی همیشه ۲۸px است و روی صفحهٔ لمسی ۴۴px. اندازه‌گیری شد:
±۱۲px در هر چهار جهت.

**چهار ورودی برچسب نداشتند** — `setupData`، `cfName`، `cfUrl`، `fuText`.
placeholder برچسب نیست: با شروع تایپ ناپدید می‌شود و اسکرین‌ریدر هم آن را نمی‌خواند.

**یک `</div>` اضافی در مودالِ «بپرس از AI»** که مرورگر بی‌صدا تحملش می‌کرد و ساختار
را یک لایه به‌هم می‌ریخت. حالا هر ۱۲ نوع تگ متعادل است.

**تنظیمات سه دسته شد.** ۹ آکاردئونِ پشت‌سرهم بودند و سه‌تای آخر عملاً پیدا نمی‌شدند:
**اتصال‌ها** · **شخصی‌سازی** · **داده**.

**دو چیز به اسم «پل» کنار هم بود.** «پل منشی» شد **«اتصال به اکستنشن منشیِ قدیمی»**
با توضیحِ صریح که فقط برای کسی است که نسخهٔ جداگانهٔ قدیمی را هم نصب دارد. چهار
متنِ کاربرپسند هم هنوز نامِ قدیمیِ محصول («وردست») را داشتند که پاک شد.

**`core/agenda.js`** — منطقِ خالصِ برنامهٔ روز از `app.js` بیرون آمد: وقت آزاد،
سریِ جلسه‌های تکراری، تطبیق جلسه با رویدادِ تقویم، و نرمال‌سازیِ جست‌وجو. این‌ها
منطقِ واقعیِ محصول بودند و **هیچ تستی نداشتند**؛ حالا ۱۸ تست دارند. `app.js` هم
۸۳ خط کوتاه‌تر شد.

> دربارهٔ شکستنِ کاملِ `app.js`: اندازه گرفتم — **۳۰۹ شناسهٔ مشترک و ۲۲۸۲ ارجاع
> متقابل** در یک IIFE. تخمینِ «یک روزِ» اولیه غلط بود؛ این یک پروژهٔ چندروزه با
> ریسکِ واقعیِ خرابیِ خاموش است، چون هیچ تستِ رابطی وجود ندارد که رگرسیون را بگیرد.
> فعلاً همان بیرون‌کشیدنِ تدریجیِ منطقِ خالص ادامه دارد.

**باگِ ویندوز — هشدارِ همگام‌سازی اصلاً کار نمی‌کرد.** الگوها فقط `/` را می‌شناختند،
پس مسیرِ ویندوزیِ `C:\Users\ali\Dropbox\manshi` **سبز** نشان داده می‌شد با متنِ
«فقط روی همین دستگاه می‌ماند». یعنی نه‌تنها هشدار نمی‌داد، بلکه آرامشِ دروغین می‌داد —
از نداشتنِ هشدار بدتر. حالا جداکننده‌ها یکسان می‌شوند و `C:\Users\…` و `/home/…`
هم مثل مسیرِ مک بررسی می‌شوند. تلهٔ «OneDrive Backup» ویندوز هم مثل تلهٔ
«Desktop & Documents» مک نام برده شد.

**راهنما سیستم‌عامل‌دار شد.** یک منوی «مک / ویندوز / لینوکس» که خودش حدس می‌زند ولی
قفل نیست (شاید کسی روی مک برای همکارِ ویندوزی‌اش تنظیمات بسازد). با تغییرش:

- مسیرِ فایلِ تنظیمات عوض می‌شود — `%APPDATA%\Claude\…` به‌جای `~/Library/Application Support/Claude/…`، و برای لینوکس `~/.config/Claude/…`
- نقلِ پوسته عوض می‌شود: ویندوز کوتیشنِ **دوتایی** می‌خواهد چون `cmd.exe` تک‌نقل را عیناً چاپ می‌کند
- «در ترمینال اجرا کن» می‌شود «در PowerShell اجرا کن»
- جداکنندهٔ مسیرِ اسکریپت از خودِ مسیر گرفته می‌شود، تا `C:\…\manshi/mcp/…` مخلوط درنیاید

**سرور روی ویندوز امن‌تر می‌نویسد.** `renameSync` روی ویندوز اگر فایل مقصد را برنامهٔ
دیگری باز نگه داشته باشد `EPERM` می‌دهد. حالا در آن حالت مستقیم می‌نویسد — اتمی نیست
ولی از شکست بهتر است.

**راهنما حالا می‌گوید کجای کاری.** بالای بخش یک شمارنده هست («۲ از ۵ قدم») و کنارِ
هر قدم یا تیکِ سبز است، یا حلقهٔ «الان اینجایی»، یا خاکستریِ «هنوز نه». رنگ تنها
نشانه نیست — شماره هم به تیک تبدیل می‌شود.

سه قدم را خودِ منشی می‌فهمد: مسیر پر شد، تنظیمات کپی شد، و **اولین صورت‌جلسه از
سرور رسید** — این آخری قوی‌ترین نشانه است، چون یعنی کلِ زنجیره (Node، تنظیمات،
ابزار، سرور) کار کرده. دو قدمِ دیگر را نمی‌شود فهمید (Node نصب است؟ ابزار را باز
کردی؟) پس کاربر خودش تیک می‌زند.

قدم‌ها و مسیرِ چسبانده‌شده در تنظیمات می‌مانند، پس با بستن و باز کردن از صفر شروع
نمی‌شود.

**زبانِ بخش ساده‌تر شد.** «کوئری کردن» و «فایلِ snapshot» و «سرور MCP» از عنوان‌ها
رفتند. حالا دو راه اسم دارند: **«کپی کن و بچسبان»** با برچسبِ «برای همه · بدون نصب»،
و **«اتصال مستقیم»** با برچسبِ «کارِ فنی دارد» — تا کاربر از همان نگاهِ اول بداند
کدام مالِ اوست.

**دو فرضِ فنی که راهنما داشت، برداشته شد.** «Node نصب باشد» از بخشِ عیب‌یابی — یعنی
جایی که فقط بعد از شکست دیده می‌شود — به **گام صفر** آمد، با دستورِ بررسی. و فیلدِ
«پوشهٔ منشی (جایی که کلونش کردی)» شد «پوشهٔ خودِ منشی»، چون کاربر لازم نیست گیت بلد
باشد: همان پوشه‌ای است که اکستنشن را از آن بار کرده، و اگر یادش نباشد کروم در
`chrome://extensions` زیرِ «Loaded from» نشانش می‌دهد.

بالای بخش هم صریح نوشته شد کدام مسیر برای چه کسی است: **«بپرس از AI» برای همه،
فایلِ snapshot کارِ فنی می‌خواهد.**

**مرجعِ ابزارها، با جمله‌هایی که واقعاً می‌نویسی.** بخشِ تازه‌ای در راهنما: یک جدولِ
«می‌خواهی ← این را بنویس» و زیرش فهرست پنج ابزاری که منشی در اختیار مدل می‌گذارد،
با توضیح فارسی. `write_minutes` — تنها ابزاری که می‌نویسد — برچسبِ جدا دارد.

بالای جدول صریح نوشته شده که **لازم نیست نامِ ابزارها را بلد باشی**؛ به فارسی بگو
چه می‌خواهی و مدل خودش انتخاب می‌کند. نام‌ها فقط برای وقتی‌اند که مدل راه را گم کند.

**و یک بازآراییِ زیرِ پوست:** تعریفِ ابزارها به `core/mcp-tools.js` منتقل شد و حالا
**هم سرور و هم راهنما از همان یک فایل می‌خوانند**. قبلاً دو نسخه بود و دیر یا زود
از هم می‌افتادند — یعنی کاربر راهنمایی می‌دید که با رفتارِ واقعیِ سرور نمی‌خواند.
یک تست هم همین را قفل می‌کند: مطمئن می‌شود سرور فهرستِ جداگانه‌ای تعریف نکرده و هر
ابزارِ تعریف‌شده پیاده‌سازی دارد.

**راهنمای پل، کامل‌تر.** ابزارها از پنج به هفت رسید — Windsurf و یک گزینهٔ عمومیِ
«ابزار دیگر» برای هر برنامه‌ای که قالبِ استانداردِ `mcpServers` را می‌پذیرد. عمداً
فقط ابزارهایی را نام بردم که از شکلِ تنظیماتشان مطمئنم؛ بقیه با همان گزینهٔ عمومی
پوشش داده می‌شوند. انتخابِ ابزار از چیپ به **فهرست کشویی** تغییر کرد و برای هرکدام
یادداشتِ جای فایل هم می‌آید (مثلاً اینکه اپ و افزونه و CLIِ Codex همه از یک فایل
می‌خوانند، یا اینکه کلیدِ VS Code به‌جای `mcpServers` می‌شود `servers`).

دو گام تازه: **«ببند و باز کن»** با روشِ اطمینان از نصب (در Claude Code همان `/mcp`)،
و **«کار را انتخاب کن»** با پنج سؤالِ آماده به‌جای یکی. یک بخشِ **عیب‌یابی** هم پایینش
آمد: Node نصب نیست، ابزار در فهرست نیست، مسیر اشتباه، متن در فایل نیست، و اینکه
صندوق ورودی فقط وقتی خوانده می‌شود که صفحهٔ منشی باز باشد.

**لحنِ هشدارِ مسیر عوض شد.** قبلاً می‌گفت «جای دیگری انتخاب کن» — این تصمیمِ کاربر
است، نه من. گذاشتنِ پوشه در iCloud می‌تواند انتخابِ آگاهانه باشد (دسترسی از دستگاه
دیگر). حالا فقط می‌گوید داده کجا می‌رود و اضافه می‌کند «اگر آگاهانه همین را می‌خواهی،
هیچ ایرادی ندارد». رنگ قرمز هم برداشته شد؛ این خبر است، نه خطا.

**بازرس امنیتی حالا نامِ کاربریِ دستگاه را هم می‌گیرد.** مسیرهایی مثل
`/Users/<نام>` در نمونه‌ها و تست‌ها جا می‌مانند — دقیقاً همین شد و کاربر گرفتش،
نه اسکریپت. حالا `id -un` را در فایل‌های staged می‌گردد.

**راهنمای وصل‌کردن، داخل خودِ منشی.** قبلاً پوشه را انتخاب می‌کردی و تمام — بعدش
کاربر نمی‌دانست چه کند. حالا بعد از انتخاب پوشه، سه قدم می‌آید: مسیر را بچسبان،
ابزارت را انتخاب کن (Claude Code / Claude Desktop / Codex / Cursor / VS Code)، و
تنظیماتِ آماده را کپی کن. سؤالِ شروع هم با یک دکمه کپی می‌شود.

**و هشدارِ پوشهٔ همگام‌شونده، این بار واقعی.** هشدارِ متنیِ قبلی («در دراپ‌باکس نگذارید»)
جلوی یک مورد واقعی را نگرفت: کاربر پوشه‌ای در `~/Documents` ساخت و چون روی مک
«Desktop & Documents» در iCloud روشن بود، ۲٫۸ مگابایت متنِ کلمه‌به‌کلمهٔ جلسه‌های
واقعی شروع کرد به رفتن روی iCloud — و Finder هیچ نشانه‌ای نشان نمی‌داد.

اکستنشن مسیرِ پوشه را نمی‌بیند (`showDirectoryPicker` فقط نام می‌دهد)، پس تشخیصِ
خودکار ممکن نیست. ولی حالا که کاربر مسیر را برای ساختِ تنظیمات می‌چسباند، همان‌جا
بررسی می‌شود: iCloud، دراپ‌باکس، گوگل‌درایو، وان‌درایو، نکست‌کلود و بقیه **قرمز**؛
و هر پوشه‌ای داخل `Documents` یا `Desktop` **زرد**، با توضیحِ دقیقِ همان تلهٔ مک.

**۱۵۸ تست تازه** (جمعاً ۴۶۷) روی `core/snapshot.js` و `core/inbox.js` — از جمله تزریق
از راه صندوق ورودی، تعارض، و سقف‌ها.

---

## ۱٫۰٫۱ — باگِ ذخیرهٔ تنظیمات و آب‌وهوای چندشهری

**باگ (گزارشِ کاربر):** تیکِ آب‌وهوا را می‌زدی، ذخیره می‌کردی، رفرش که می‌کردی تیک می‌پرید و در صفحهٔ امروز هم چیزی نشان داده نمی‌شد.

علتش این بود که هنگام افزودنِ گزینه، الگویی که در کدِ ذخیره جایگزین می‌کردم دیگر با متنِ فایل نمی‌خواند و **جایگزینی بی‌صدا انجام نشد** — چک‌باکس حالتش را عوض می‌کرد ولی `saveSettings` هیچ‌وقت `weatherOn` را نمی‌نوشت. همان اتفاق برای `updateCheckOn` هم افتاده بود.

سرِ راه یک باگِ دوم هم پیدا شد که هنوز به چشم نیامده بود: `weatherCities` در `heroPrefs` مقداردهی نمی‌شد، پس **هر بار ذخیرهٔ تنظیمات، فهرست شهرها را پاک می‌کرد**.

**آب‌وهوا کامل شد:**
- **چند شهر هم‌زمان** (تا ۶ تا) از ۱۵ شهر ایران — همه در **یک درخواست**.
- برای هر شهر: دمای فعلی، وضعیت به فارسی، بیشینه/کمینهٔ امروز، و رطوبت و باد روی نگه‌داشتنِ نشانگر.
- نشانهٔ تصویری برای هر وضعیت (آفتابی، ابری، باران، برف، رعدوبرق، مه…) از روی کدهای WMO.
- انتخاب شهرها روی خودِ کارت، بدون رفتن به تنظیمات.
- سربرگِ «امروز» دمای شهر اول را نشان می‌دهد.
- تازه‌سازی نیم‌ساعت یک بار، نه بیشتر.

پاسخِ تک‌شهریِ سرویس شیء است و چندشهری آرایه — هر دو حالت خوانده می‌شود و پاسخِ خراب هیچ ردیفی نمی‌دهد.

۹ تست تازه؛ جمعاً ۳۰۹.

---

## ۱٫۰٫۰ — انتشار عمومی

پیش از عمومی‌کردن، یک بازرسی امنیتیِ سخت‌گیرانه روی کلِ کد و **کلِ تاریخچهٔ گیت** انجام شد. دو یافتهٔ واقعی داشت:

**۱. آب‌وهوا بدون اجازه به اینترنت وصل می‌شد.**
`loadWeather()` روی هر بار باز شدن برنامه اجرا می‌شد — بدون هیچ گزینه‌ای، بدون اطلاع کاربر. درخواست هیچ دادهٔ کاربری نمی‌فرستاد (مختصات تهران ثابت بود)، ولی **IP کاربر را به سرویس بیرونی نشان می‌داد** و مستقیماً با ادعای README تناقض داشت که می‌گفت آب‌وهوا اختیاری و پیش‌فرض خاموش است.
حالا گزینهٔ جداگانه دارد و پیش‌فرض خاموش است.

**۲. فونت وزیرمتن بدون پروانه منتشر می‌شد.**
`Vazirmatn.woff2` تحت **SIL OFL 1.1** است و این پروانه انتشارِ متنِ پروانه همراه فونت را الزامی می‌کند. فایل `fonts/NOTICE.md` با نام پدیدآور، مخزن و پروانه اضافه شد و در README هم آمد.

**شفافیت دربارهٔ تنها درخواستِ پیش‌فرض‌روشن.**
بررسی نسخه پیش‌فرض روشن است. به‌جای پنهان‌کردنش، صریح در README نوشته شد که چیست، چه می‌فرستد (هیچ) و چرا این استثنا منطقی است — و گزینهٔ خاموش‌کردنش هم اضافه شد.
با همهٔ گزینه‌ها خاموش، **صفر درخواست بیرونی** ثبت شد (اندازه‌گیری‌شده).

**آنچه بازرسی تأیید کرد:**
- هیچ کلید، توکن، JWT یا کلید خصوصی در هیچ کامیتی
- هیچ نام، ایمیل یا متنِ جلسهٔ واقعی در هیچ کامیتی — همهٔ ایمیل‌ها روی دامنه‌های رزروشدهٔ استاندارد
- هیچ فراداده در PNGها و فونت
- هیچ `eval`، `new Function`، `document.write` یا `innerHTML` با دادهٔ متغیر
- هیچ وابستگی، `node_modules` یا اسکریپت راه‌دور
- `externally_connectable` تعریف‌نشده و گیرندهٔ پیام بیرونی با شناسهٔ فرستنده محدود شده
- دادهٔ نمونه با `if (isExt) return` هرگز به اکستنشن نمی‌رسد
- هیچ `console.log`، `TODO`، `FIXME` یا کدِ مرده

۳۰۰ تست، بدون وابستگی.

---

## ۰٫۹٫۷ — خبرِ نسخهٔ تازه و نشانِ نو

**نشانِ منشی عوض شد** — حبابِ گفتار با تیکِ داخلش، دنباله و یک درخشش، روی مربعِ گردگوشهٔ سبز. هم `icon16/32/48/128.png` و هم نشانِ داخلِ برنامه از **یک هندسهٔ واحد** ساخته می‌شوند، پس هیچ‌وقت از هم جدا نمی‌افتند.

**خبرِ نسخهٔ تازه.**
منشی از فروشگاه کروم نصب نمی‌شود، پس خودش به‌روز نمی‌شود — تا حالا راهی نبود که بفهمی نسخهٔ تازه‌ای آمده.
- روزی یک بار، بی‌سروصدا، آخرین ریلیزِ گیت‌هاب بررسی می‌شود. اگر نسخهٔ بالاتری بود، بالای صفحهٔ «امروز» بنر می‌آید با لینکِ دانلود و دکمهٔ «بعداً».
- «بعداً» فقط همان نسخه را ساکت می‌کند؛ نسخهٔ بعدی دوباره خبر می‌دهد.
- در تنظیمات، کنارِ شمارهٔ نسخه، دکمهٔ **«بررسی نسخهٔ تازه»** برای وقتی خودت می‌خواهی چک کنی.
- مقایسهٔ نسخه عددی است، نه رشته‌ای: **۰٫۹٫۱۰ از ۰٫۹٫۹ تازه‌تر** حساب می‌شود.
- آدرسِ دانلود از پاسخِ گیت‌هاب می‌آید، پس بررسی می‌شود: فقط `https` و فقط روی خودِ `github.com`؛ وگرنه به صفحهٔ رسمی برمی‌گردد.
- اگر دسترسی داده نشده یا گیت‌هاب پاسخ ندهد، هیچ اتفاقی نمی‌افتد — هیچ خطایی به کاربر نشان داده نمی‌شود.

**باگی که سرِ راه گرفته شد:** بررسیِ نسخه گاهی «نسخهٔ تو ۰٫۰٫۰ است» می‌گفت، چون نسخهٔ خودمان از DOM خوانده می‌شد و خواندنِ `manifest.json` ناهمگام است. حالا یک بار خوانده و نگه داشته می‌شود، و تا وقتی معلوم نشده هیچ ادعایی نمی‌شود.

۱۴ تست تازه؛ جمعاً ۳۰۰.

---

## ۰٫۹٫۶ — چند منبعِ نقل‌قول و افزودنِ منبعِ خبرِ دستی

**نقل‌قول: سه منبع به‌جای یکی.**
`ZenQuotes` → `DummyJSON` → `Type.fit` — به ترتیب امتحان می‌شوند و اولی که جواب داد برنده است. شکلِ پاسخِ هر سه فرق دارد (`q/a` ، `quote/author` ، `text/author`) و همه خوانده می‌شوند.
اگر هیچ‌کدام جواب ندهد، پیام می‌گوید هرکدام دقیقاً چه گفتند. اجازهٔ هر سه دامنه یک‌جا گرفته می‌شود، پس وقتی یکی از کار می‌افتد جایگزینش بدونِ پرسیدنِ دوباره کار می‌کند.

**منبع خبرِ دستی.**
حالا می‌توانی هر فید RSS/Atom دیگری را خودت اضافه کنی — در تنظیمات ← سربرگ صفحهٔ امروز ← «منبع خودت را اضافه کن».
- نام دلخواه (اگر خالی بگذاری نامِ دامنه می‌شود) و انتخابِ دسته؛ در همان دسته کنارِ منبع‌های آماده می‌نشیند.
- **عکس خبر هم می‌آید** — همان مسیرهای همیشگی: `enclosure`، `media:content`، `media:thumbnail`، `<image>` یا اولین `<img>` توضیحات.
- **قبل از افزوده‌شدن آزمایش می‌شود.** اجازهٔ دامنه گرفته، فید واقعاً خوانده، و اگر خبری نداشت **اضافه نمی‌شود** — منبعِ مرده در فهرست نمی‌ماند. پیام می‌گوید چند خبر و چندتا با عکس.
- ورودی سخت‌گیرانه بررسی می‌شود: فقط `https`، آدرسِ سالم، بدون تکرار، سقف ۱۲ منبع، نامِ بریده‌شده و دستهٔ معتبر. `javascript:` و `http:` رد می‌شوند.
- حذف، هم منبع را می‌برد هم از فهرستِ فعال بیرونش می‌کند.

۱۱ تست تازه؛ جمعاً ۲۸۶.

---

## ۰٫۹٫۵ — تایمر تمرکز و سخنِ به‌روزشونده

**تایمر تمرکز** — کارتِ تازهٔ کیوسک، و تنها کارتی که به بقیهٔ منشی وصل است.
یک کار انتخاب می‌کنی، دورِ ۲۵ (یا ۱۵/۵۰) دقیقه‌ای می‌گذاری، و وقتی تمام شد **همان دقیقه‌ها روی خودِ کار ثبت می‌شود**. کنارِ «تخمین زمان» که قبلاً ساختیم معنا پیدا می‌کند: «تخمین ۴۵ دقیقه · تا حالا ۱۲ دقیقه کار شده».
- بعد از هر دور استراحتِ ۵ دقیقه‌ای، و هر ۴ دور استراحتِ ۱۵ دقیقه‌ای.
- «امروز ۳ دور · ۷۵ دقیقه تمرکز» پایین کارت؛ با عوض‌شدنِ روز خودش صفر می‌شود.
- **شمارش از ساعتِ دیوار است، نه از تیکِ داخلی.** مرورگر تایمرِ تبِ پنهان را کند می‌کند؛ با این روش اگر تب را ببندی و برگردی، عدد همچنان درست است. تست شد.
- شمارندهٔ یک‌ثانیه‌ای فقط وقتی جلسه‌ای در جریان است روشن می‌شود.

**سخن روز — باگِ ظاهری**
دکمه‌های «همه / شعر / نقل‌قول» کلاسی می‌گرفتند که در CSS **وجود نداشت**، پس خامِ مرورگر رندر می‌شدند و بدشکل بودند. کلاس‌های خودشان ساخته شد.

**سخن روز — به‌روزرسانی**
- گزینهٔ اختیاریِ «سخن‌های تازه از اینترنت بگیر» (پیش‌فرض خاموش). نقل‌قول‌های انگلیسی از `zenquotes.io` گرفته و **محلی ذخیره** می‌شوند، پس مجموعه با گذشتِ زمان بزرگ‌تر می‌شود و بدون اینترنت هم می‌ماند.
- مجموعهٔ فارسیِ داخلی همیشه سرِ جایش است؛ این فقط رویش اضافه می‌شود.
- پاسخِ سرویس سخت‌گیرانه پالایش می‌شود: خیلی کوتاه، خیلی بلند، بی‌گوینده و ردیفِ تبلیغاتی رد می‌شوند. پاسخِ خراب صفحه را نمی‌شکند.
- نقل‌قولِ انگلیسی چپ‌به‌راست و با خطِ تیرهٔ نامِ گوینده نمایش داده می‌شود.
- تازه‌سازیِ خودکار هر ۱۲ ساعت در پس‌زمینه، بی‌سروصدا؛ سقفِ نگه‌داری ۳۰۰ سخن.

۲۲ تست تازه. کنتراست در روشن و تیره سنجیده شد.

---

## ۰٫۹٫۴ — نشانِ تازه، رنگ، سخنِ روز و ساعت مسقط

**نشانِ منشی عوض شد.** تیکِ داخلِ مربع شبیهِ هر اپلیکیشن کارها در دنیا بود. حالا **«م» فارسی** است: سرِ گرد با دنبالهٔ رو به چپ، روی زمینهٔ گرادیانِ سبز. هم هویتِ فارسی دارد، هم در ۱۶ پیکسل خوانا می‌ماند.
آیکن‌های `icon16/32/48/128.png` هم از همان هندسه بازتولید شدند، پس آیکنِ نوارِ مرورگر هم عوض می‌شود — نه فقط داخلِ برنامه.

**رنگ به طراحی اضافه شد — به‌عنوان ساختار، نه تزئین:**
- هر کارتِ کیوسک نوارِ باریکِ رنگی و عنوانِ هم‌رنگِ خودش را دارد: تقویم آبی، اوقات فیروزه‌ای، سخن بنفش، بازار کهربایی، اخبار سبز. چشم سریع‌تر کارتِ موردنظر را پیدا می‌کند.
- نوار «یک‌نگاه» هر سنجه رنگِ خودش را گرفت.
- سربرگِ امروز شست‌وشوی بسیار ملایمِ رنگی گرفت به‌جای زمینهٔ تخت.
- کنشِ اصلی و نشانِ برند همان گرادیانِ هویت را دارند.
دو رنگِ تازه (بنفش و فیروزه‌ای) به پالت اضافه شد، هر دو در پوستهٔ روشن و تیره جداگانه تنظیم شده.

**«بیت روز» شد «سخن روز».** از ۱۴ بیتِ حافظ به **۴۲ سخن از ۲۰ گوینده**:
- شعر: حافظ، سعدی، مولوی، خیام، فردوسی، نظامی، باباطاهر، پروین اعتصامی، شهریار
- نقل‌قول: انیشتین، ماری کوری، ادیسون، ماندلا، جابز، آلن کی، سقراط، لائوتزه، مارکوس اورلیوس، هلن کلر، دراکر
- سه دکمهٔ «همه / شعر / نقل‌قول» و «یکی دیگر» که تضمین می‌شود همان قبلی را ندهد.
- عمداً فقط نقل‌قول‌هایی آمده که انتسابشان محلِ مناقشه نیست؛ جمله‌های پرتکرارِ اینترنتی که به آدم‌های بزرگ نسبت داده می‌شوند ولی سندی ندارند، کنار گذاشته شدند.

**ساعت مسقط** اضافه شد و اول فهرست نشست. نمایشِ هم‌زمانِ چند ساعت از قبل کار می‌کرد ولی راهنمای تنظیمات خلافش را می‌رساند — متنش اصلاح شد: «هر تعداد شهر که خواستی انتخاب کن».

۵ تست تازه برای سخن روز. کنتراست همهٔ عناصرِ رنگی در روشن و تیره سنجیده شد: هیچ موردی زیر حدِ AA نیست.

---

## ۰٫۹٫۳ — چیدمانِ کیوسک

کارت‌ها در یک شبکهٔ ردیفی بودند، پس **بلندترین کارتِ هر ردیف ارتفاعِ کلِ ردیف را تعیین می‌کرد**. «بازار» بلند بود، زیرِ «تقویم» و «بیت روز» حفرهٔ بزرگی می‌ماند و «اخبار» به ته صفحه پرت می‌شد.

- شبکه به **چیدمانِ ستونی** تغییر کرد؛ کارت‌ها پشتِ سرِ هم می‌ریزند و فضای عمودی پر می‌شود. ارتفاعِ صفحه از ۱۷۱۳ به ۱۳۳۵ رسید و حفرهٔ میانی از بین رفت.
- سقفِ دو ستون گذاشته شد تا روی نمایشگرِ بزرگ، کارت‌ها باریک و ناخوانا نشوند. زیر ۷۶۰ پیکسل یک‌ستونه می‌شود.
- هیچ کارتی وسطش بین دو ستون نصف نمی‌شود.
- **شمارش معکوس‌ها دو‌در‌دو شدند.** پیش‌تر سه‌تا کنار هم می‌نشست و چهارمی تنها به خط بعد می‌افتاد.
- **فهرست اخبار به شش خبر کوتاه شد** با دکمهٔ «۱۴ خبر دیگر». هم کارت را از غول‌شدن درآورد، هم به همان قرارِ «این صفحه باید ته داشته باشد» وفادار است.
- ردیف‌های بازار کمی جمع‌تر شدند و توضیحِ سه‌خطیِ پایینش به یک خط رسید (بقیه‌اش روی نگه‌داشتنِ نشانگر).

---

## ۰٫۹٫۲ — بازار، منابع خبر و عکسِ خبر

**کارت بازار** — دلار، یورو، درهم، پوند، لیر · مثقال و طلای ۱۸ عیار · سکهٔ امامی، بهار آزادی، نیم، ربع، گرمی · انس طلا و نقره.

دو تصمیم که ارزش گفتن دارد:

- **هیچ متنی از سایتِ بیرونی وارد رابط نمی‌شود.** نامِ هر قلم از جدولِ خودمان می‌آید و فقط *عدد* از صفحه خوانده می‌شود. تست شده: ردیفی با `<img onerror=…>` در نامش، برچسبِ سالمِ خودمان را می‌گیرد.
- **درصد تغییرِ ارز را خودمان می‌سازیم.** منبع برای ارز درصد نمی‌دهد؛ به‌جای وابسته‌شدن به سرویس دوم، هر بار قیمت ذخیره می‌شود و تغییر از مقایسه با روزِ قبل درمی‌آید. ۳۰ روز نگه داشته می‌شود — کاملاً محلی. برای طلا و سکه، درصدِ خودِ منبع مقدم است.

در دادهٔ واقعی «سکه گرمی: ‎−۹۷٪» دیده شد که آشکارا خرابیِ منبع است؛ درصدِ بالای ۴۰٪ به کاربر نشان داده نمی‌شود.

**منابع خبر از ۲ به ۹ رسید، دسته‌بندی‌شده:**
فناوری (زومیت، دیجیاتو، آی‌تی ایران) · ورزشی (ورزش سه، طرفداری) · اقتصاد (دنیای اقتصاد، اقتصادنیوز) · عمومی (خبرآنلاین، ایسنا).
در کیوسک بالای فهرست، دکمهٔ دسته آمد — و فقط دسته‌هایی که واقعاً خبر دارند دیده می‌شوند. انتخابگرِ تنظیمات هم دسته‌بندی شد، چون فهرستِ تختِ ۹تایی خوانده نمی‌شد.

**عکسِ خبر** — از هر جایی که فید گذاشته باشد: `enclosure`، `media:content`، `media:thumbnail`، `<image>`، یا اولین `<img>` داخل توضیحات. هر سه مسیر تست شد.
امنیت: فقط `https`، و فقط چیزی که واقعاً عکس است. `javascript:` و `data:` رد می‌شوند. عکس‌ها با `referrerpolicy="no-referrer"` بار می‌شوند تا آدرسِ منشی برای سرورِ عکس نرود، و `lazy` هستند. عکسِ خراب خودش را برمی‌دارد و جای خالی نمی‌گذارد.
بندانگشتی ۶۸×۵۰ کنارِ عنوان است، نه بالای آن — فهرست فشرده می‌ماند. کارتِ صفحهٔ «امروز» عمداً بی‌عکس ماند تا آن صفحه آرام بماند.

**تقویم** — «پیروزی انقلاب اسلامی» حذف شد و جایش **«آخر هفته»** آمد: تنها شمارشی که هر روز عوض می‌شود و به برنامه‌ریزی می‌آید. (۶ مرداد سه‌شنبه است → «۲ روز».)

۲۶ تست تازه؛ جمعاً ۲۴۸.

---

## ۰٫۹٫۱ — صفحهٔ «کیوسک»

صفحهٔ مکث، در ریل کنار بقیه. **هرچه اینجاست روی همین دستگاه حساب می‌شود** — هیچ درخواست شبکه‌ای، جز کارت اخبار که اختیاری و پیش‌فرض خاموش است.

**تقویم و مناسبت‌ها** — مناسبت امروز، شمارش معکوس (پایان ماه، پایان فصل، تعطیلی بعدی، نوروز) و پنج مناسبت پیشِ رو با تاریخ. سرریزِ اسفند به فروردین تست شده، پس اواخر سال فهرست خالی نمی‌شود.
عمداً **فقط مناسبت‌های تقویم شمسی**. مناسبت‌های قمری هر سال جابه‌جا می‌شوند و بدون رؤیت هلال دقیق نیستند؛ حدس‌زدنشان بدتر از نگفتنشان بود، و همین را روی خودِ کارت نوشته‌ام.

**اوقات شرعی** — کاملاً محاسباتی، به روشِ مؤسسهٔ ژئوفیزیک دانشگاه تهران (فجر ۱۷٫۷°، مغرب ۴٫۵°، عشا ۱۴°) و بدون ساعت تابستانی. ۱۵ شهر. «تا اذان مغرب ۳ ساعت و ۱۲ دقیقه» بالای کارت.
درستی‌اش با واقعیت سنجیده شد: طول روزِ تهران در ۲۸ ژوئیه ۱۴ ساعت و ۳ دقیقه درمی‌آید — همان عددِ واقعی. عرض‌های قطبی (که خورشید طلوع نمی‌کند) به‌جای NaN، «—» می‌دهند.

**بیت روز** — یک بیت از حافظ، هر روز تازه و در طولِ روز پایدار. «یک فال دیگر» و «کپی».

**اخبار** — همان فید، اینجا به‌شکل فهرستِ کامل (تا ۱۰ خبر با منبع و زمان) به‌جای تیترِ چرخانِ صفحهٔ امروز. کارتِ فشردهٔ «امروز» سرِ جایش ماند.

**هر کارت اختیاری است.** دکمهٔ «چه چیزهایی دیده شود» روی خودِ صفحه — نه سفر به تنظیمات. پیش‌فرض: تقویم و بیت روشن، اوقات و اخبار خاموش.

۲۱ تست تازه. کنتراست همهٔ عناصر در روشن و تیره سنجیده شد: هیچ موردی زیر حدِ AA نیست.

---

## ۰٫۹٫۰ — «کارها» حرفه‌ای شد

پنج تغییر، همه با تست. کارها دیگر فقط فهرستِ کارهای جلسه‌ها نیست.

**۱. روتین‌ها حافظه گرفتند (باگ)**
نمونهٔ بعدیِ یک کار تکرارشونده فقط عنوان و تکرار را می‌برد. یعنی چک‌لیستِ «بستن ماه مالی» هر بار از صفر. حالا برچسب، زیرکارها، توضیحات، سنجاق و تخمینِ زمان همه منتقل می‌شوند — زیرکارها **باز** و با شناسهٔ تازه، چون رخدادِ تازه‌ای است. وقتِ زمان‌بندی‌شده منتقل نمی‌شود (مالِ همان روز بود).

**۲. پیگیری با سن و تلنگر**
هر کارِ سپرده‌شده حالا وضعیت دارد: تازه ← بی‌جواب (۳ روز) ← بی‌خبر (۷ روز) ← از موعد گذشته. ترتیبِ فهرست هم عوض شد: اول آنچه از موعد گذشته، بعد آنچه بیشتر بی‌خبر مانده.
- کپی‌کردنِ «پیش‌نویس یادآوری» **خودش تلنگر را ثبت می‌کند** — کارِ دستیِ اضافه ندارد.
- دکمهٔ زنگوله برای وقتی که بیرون از منشی پیگیری کرده‌ای (زنگ زدی، حضوری گفتی).
- روی نشانِ وضعیت نگه داری: «۱۰ روز از سپردن · ۲ بار تلنگر زده‌ای».

**۳. تخمینِ زمان، وصل به وقتِ آزاد**
از منوی «⋯» بگو کاری چقدر طول می‌کشد (۱۵ دقیقه تا ۳ ساعت). بعد سربرگِ امروز کنارِ «بزرگ‌ترین وقت آزاد» می‌گوید **«۳ کار (۲ ساعت) جا می‌شود»** — و با نگه‌داشتن نشانگر، اسمشان را می‌بینی. انتخاب حریصانه است: فوری‌ترین‌ها اول، و هیچ‌وقت از ظرفیتِ بازه بیرون نمی‌زند.

**۴. مرتب‌سازیِ هوشمند واقعاً هوشمند شد**
قبلاً فقط سطل‌بندیِ تاریخ بود. حالا داخلِ هر سطل امتیازِ فوریت حساب می‌شود: سنجاق، شدتِ عقب‌افتادگی، وقتِ گذاشته‌شدهٔ امروز، کارِ نیمه‌کاره، و بی‌حرکتیِ طولانی. کارِ بی‌تاریخی که ماه‌ها مانده کم‌کم بالا می‌آید — ولی هیچ‌وقت از ددلاینِ امروز بالاتر نمی‌رود.
نشانِ **«۴۵ روز بی‌حرکت»** روی ردیف می‌آید (فقط در فهرستِ کارها، نه در تمرکز امروز).

**۵. مرور کارهای پوسیده**
دکمهٔ «مرور» **فقط وقتی می‌آید که چیزی برای مرور هست**. برای هر کار سه راه: «هنوز لازمه»، تاریخ تازه، یا حذف.
معیار: بیش از ۲۱ روز بی‌حرکت، یا بیش از ۱۴ روز از ددلاین گذشته و ۱۴ روز دست‌نخورده. آخرین کار که رسیدگی شد، دکمه محو می‌شود و به «در جریان» برمی‌گردی.

**در حین ساخت گرفته شد:** «هنوز لازمه» برای کارِ از موعد گذشته هیچ کاری نمی‌کرد — دکمه را می‌زدی و ردیف سرِ جایش می‌ماند، چون معیارِ دیرکرد به دست‌زدن نگاه نمی‌کرد. حالا هر دو مسیر به «تازه بررسی‌اش کرده‌ام» احترام می‌گذارند.

کنتراستِ همهٔ عناصر تازه در پوستهٔ روشن و تیره سنجیده شد (کمینه ۴٫۶ و ۴٫۸).

---

## ۰٫۸٫۵ — نوشتنِ دستیِ صورت‌جلسه

جلسهٔ حضوری متنی ندارد که AI رویش کار کند، و تب «صورت‌جلسه» در آن حالت یک بن‌بست بود: «این جلسه متنی برای تحلیل ندارد» و تمام — هیچ راهی برای نوشتن.

- در هر حالتِ خالی، دکمهٔ **«خودم می‌نویسم»** هست. وقتی جلسه متن ندارد، همین دکمه کنشِ اصلی است.
- به‌جای صفحهٔ سفید، **اسکلتِ آماده** باز می‌شود: عنوان، تاریخ و شرکت‌کننده‌ها از خودِ جلسه پر شده، با سرفصل‌های خلاصه / موضوع‌ها / تصمیم‌ها / اقدام‌ها / جلسهٔ بعد.
- هرچه در جدولِ **اقدام‌ها** بنویسی، موقع ذخیره به تب «اقدام‌ها» هم می‌رود و از آنجا به «کارها» می‌فرستی. ذخیرهٔ دوباره تکراری‌شان نمی‌کند.
- دکمهٔ AI وقتی جلسه متن ندارد **غیرفعال** است و می‌گوید چرا — پیش‌تر می‌شد زد و درخواستِ خالی می‌رفت.
- ویرایشگرِ سند حالا یکی است (چه ویرایشِ سندِ ساخته‌شده، چه نوشتن از صفر) و هر دو `summaryEditedAt` می‌گذارند، پس «ساخت دوباره» قبل از بازنویسیِ دست‌نوشتهٔ تو تأیید می‌گیرد.

---

## ۰٫۸٫۴ — جلسه‌ای که یک نفر یک‌ریز حرف می‌زند

**باگِ اصلی: ضبط بی‌صدا قطع می‌شد.**
بلوکِ زیرنویسِ Meet متن را روی هم انباشته می‌کند. وقتی یک نفر تنها حرف می‌زند — جلسهٔ تک‌نفره، یا دو نفر با یک میکروفون — بلوک هیچ‌وقت عوض نمی‌شود و همان یک «نوبت» بی‌پایان بزرگ می‌شد تا به سقفِ ۱۲٬۰۰۰ نویسه بخورد. از آن لحظه به بعد هر زیرنویسِ تازه **انداخته می‌شد**، بدون هیچ خبری. نتیجه: پیغامِ «۱ خط ثبت شده — زیرنویس بروز نمی‌شود».

بازسازی‌اش کردم: در یک تک‌گویی ۲۱٬۴۸۹ نویسه‌ای، **فقط ۱۱٬۹۸۵ نویسه در ۱ خط** ثبت می‌شد و ۴۴٪ گفتار از بین می‌رفت. با کدِ تازه: **۲۱٬۴۸۹ نویسه در ۱۸ خط، بی‌کم‌وکاست.**

- نوبت که از ۱٬۲۰۰ نویسه گذشت، بسته می‌شود و ادامه در نوبتِ تازه می‌نشیند — به‌جای بزرگ‌شدنِ بی‌کران.
- هر بار فقط **بخشِ تازه** نوشته می‌شود، نه کلِ متنِ انباشته؛ برای همین چیزی دوباره‌نویسی یا تکرار نمی‌شود.
- سقفِ ۱۲٬۰۰۰ به ۲۰۰٬۰۰۰ رفت و دیگر «حدِ گفتار» نیست، فقط محافظِ صفحهٔ خراب است.
- نوبت‌های کوتاه‌تر یعنی ارجاعِ شواهد (T۱، T۲…) هم دوباره معنا دارد.

**پیغام‌ها**
- «زیرنویس بروز نمی‌شود» یک تشخیصِ نادرست بود؛ ما فقط می‌دانیم چیزی نشنیده‌ایم. حالا می‌گوید **«۵ دقیقه چیزی شنیده نشد»** و راه‌حل را هم می‌دهد.
- **«· برای پایان بزن» از کنارِ هشدار برداشته شد.** همان چیزی که در تصویرِ تو دیده می‌شد: کنارِ هشدارِ نارنجی خوانده می‌شد «جلسه دارد تمام می‌شود». حالا فقط در حالتِ عادی می‌آید. (تستِ قبلی این را نمی‌گرفت چون فقط متنِ حالت را می‌دید، نه برچسبِ نهایی — حالا خودِ تابعِ برچسب تست می‌شود.)

---

## ۰٫۸٫۳ — نادیده‌گرفتنِ سرِنخ‌ها و یکدست‌شدن تنظیمات

**«از جلسه‌های اخیر چه ماند»**
- هر ردیف دکمهٔ **×** گرفت. سرِنخ‌ها تلنبار نمی‌شوند: هر چه را نمی‌خواهی می‌بندی و دیگر پیدایش نمی‌شود.
- نادیده‌گرفتن **روی خودِ مورد** ثبت می‌شود، نه روی ردیف. یعنی اگر بعداً اقدامِ تازه‌ای از جلسه‌ای دربیاید، همان ردیف دوباره می‌آید — فقط با موردهای جدید. (تست شد: ۴ اقدامِ نادیده‌گرفته ساکت ماندند، اقدامِ تازه با شمارهٔ ۱ برگشت.)
- بعد از زدن ×، پیامِ **«نادیده گرفته شد» با دکمهٔ واگرد** می‌آید؛ اگر اشتباهی زدی، برمی‌گردد.
- اگر آخرین ردیف هم بسته شود، کلِ کارت جمع می‌شود و جای خالی نمی‌گذارد.
- کلیدهای نادیده‌گرفته پس از **۳۰ روز** خودکار پاک می‌شوند تا فهرست بی‌نهایت بزرگ نشود.

**تنظیمات**
- «سربرگ صفحهٔ امروز» و «میان‌برهای صفحه‌کلید» بیرون از شبکهٔ تنظیمات و بدون قالب رها شده بودند — متن لخت روی زمینه. حالا مثل بقیهٔ بخش‌ها **کارتِ تاشو** هستند: همان کادر، همان پیکان، همان عرض.

---

## ۰٫۸٫۲ — جای کارت اخبار و فاصلهٔ تازه‌سازی

- کارت اخبار **بین سربرگ و بنر پایان روز** نشست (پیش‌تر پایین‌تر بود).
- فاصلهٔ گرفتن فید تازه از **۱۵ به ۱۰ دقیقه** رسید. چرخش تیترها همان هر ۲۰ ثانیه.

---

## ۰٫۸٫۱ — تشخیصِ دقیقِ مشکل فید اخبار

**اصلاح**
- پیام «خبری خوانده نشد» به‌تنهایی نمی‌گفت مشکل کجاست. حالا وضعیتِ **هر منبع جداگانه** گزارش می‌شود و علت را دقیق نام می‌برد: دسترسی داده نشده / سایت پاسخ نداد / خطای HTTP فلان / به‌جای فید صفحهٔ وب برگشت / فید خالی بود.
- **دو دکمهٔ خودسرویس در تنظیمات:** «بررسی اتصال» که هر منبع را همان لحظه امتحان می‌کند و نتیجه را می‌گوید، و «دادن دسترسی به سایت‌ها» که پنجرهٔ اجازهٔ کروم را باز می‌کند.
- پیش از هر درخواست، وجودِ دسترسی با `chrome.permissions.contains` بررسی می‌شود تا به‌جای خطای مبهمِ شبکه، صریح بگوید دسترسی نداری.

---

## ۰٫۸٫۰ — سربرگِ کاربردی‌تر و کارت اخبار

**تازه**
- **«بزرگ‌ترین وقت آزاد» در سربرگ** — طولانی‌ترین بازهٔ خالیِ باقی‌ماندهٔ امروز، مثل «۱۹:۱۵–۲۰:۰۰ · ۴۵ دقیقه». تنها عددی که مستقیم می‌گوید همین حالا چه کاری می‌شود کرد. زیر ۳۰ دقیقه نمایش داده نمی‌شود.
- **ساعتِ شهرها قابل‌تنظیم شد.** پیش‌تر لندن و نیویورک به همه تحمیل می‌شد؛ حالا پیش‌فرض فقط ساعت خودت است و از میان ۱۰ شهر هرکدام را خواستی اضافه می‌کنی.
- **کارت «اخبار فناوری» (اختیاری)** — تیتر تازه از زومیت و دیجیاتو، هر ۲۰ ثانیه یک تیتر، با پیمایش دستی. فید هر ۱۵ دقیقه تازه می‌شود.

**حریم خصوصی**
- کارت اخبار **پیش‌فرض خاموش** است و تنها بخشی از منشی است که به سایت بیرونی وصل می‌شود. هنگام روشن‌کردن، دسترسی به همان دامنه‌ها از خودت پرسیده می‌شود و اگر ندهی، خاموش می‌ماند. فقط عنوان خبرها خوانده می‌شود — هیچ دادهٔ تو ارسال نمی‌شود.
- تیتر خبر با `textContent` درج می‌شود و لینک‌های غیر از `https` دور ریخته می‌شوند، پس فیدِ آلوده نمی‌تواند چیزی اجرا کند.

---

## ۰٫۷٫۱ — بازطراحی آیکن‌ها و کشف‌پذیریِ میان‌برها

**طراحی**
- **کلِ مجموعهٔ آیکن‌ها بازطراحی شد.** پیش‌تر چهار ضخامتِ خط هم‌زمان استفاده می‌شد (۲، ۲٫۲، ۲٫۴، ۳) و هفت اندازهٔ متفاوت — که باعث می‌شد آیکن‌های ریز سنگین‌تر از آیکن‌های درشت به‌نظر برسند. حالا همه‌جا یک ضخامت (۱٫۷۵) و یک مقیاسِ سه‌پله‌ای (۱۴ / ۱۶ / ۲۰) به‌کار می‌رود.
- هندسهٔ چند آیکنِ ضعیف بازکشیده شد: چرخ‌دندهٔ تنظیمات، فهرستِ تیک‌دارِ کارها، آدم‌ها، نمودارِ گزارش، و «سطل».
- **دو آیکنِ گمراه‌کننده عوض شدند:** «پیگیری معطل» به ساعت‌شنیِ خوانا، و «پیشرفت امروز» از شعله به نمودارِ رو به بالا.

**کاربردپذیری**
- **میان‌برهای صفحه‌کلید وجود داشتند ولی هیچ‌جا اعلام نمی‌شدند** — پس عملاً برای کاربر وجود نداشتند. حالا شمارهٔ هر بخش در تولتیپِ ریل می‌آید و فهرست کامل میان‌برها در تنظیمات هست.

---

## ۰٫۷٫۰ — جست‌وجو و پرسش در همهٔ جلسه‌ها

**تازه**
- **تبِ «جست‌وجو و پرسش»** در بخش جلسه‌ها، با یک ورودی و دو حالت:
  - **جست‌وجو** — فوری، کلیدواژه‌ای، بدون ارسال چیزی به بیرون. نتیجه‌ها بر اساس جلسه گروه‌بندی می‌شوند، هر مورد شمارهٔ خط دارد و واژه‌های پیداشده برجسته می‌شوند.
  - **پرسش از هوش مصنوعی** — پاسخ فقط از روی قطعاتِ بازیابی‌شدهٔ همان آرشیو ساخته می‌شود، با ارجاع `[۱]` به هر جلسه‌ای که استفاده شده، و سیر زمانیِ تغییرِ تصمیم‌ها وقتی موضوعی در چند جلسه دنبال شده.
- **منابع زیر پاسخ نمایش داده می‌شوند** — عنوان جلسه، تاریخ، شمارهٔ خط و متن قطعه. منابعی که واقعاً در پاسخ ارجاع شده‌اند برجسته‌اند و بقیه کم‌رنگ («۳ از ۵ قطعهٔ بررسی‌شده»). هر منبع قابل‌کلیک است و همان جلسه را باز می‌کند.
- پرامپت صریحاً حدس‌زدن را ممنوع می‌کند: اگر منابع پاسخ را نداشته باشند می‌گوید «پیدا نشد»، اگر ناقص باشند بخشِ معلوم را می‌دهد و در انتها «آنچه معلوم نشد» را می‌نویسد، و اگر منابع تناقض داشته باشند هر دو را نشان می‌دهد.

**امنیت**
- **آدرس سرویس هوش مصنوعی حالا باید https باشد.** پیش‌تر یک آدرس `http://` پذیرفته می‌شد و در آن حالت کلید API و متنِ کاملِ جلسه رمزنگاری‌نشده روی شبکه می‌رفتند. `localhost` همچنان مجاز است چون از دستگاه بیرون نمی‌رود (برای مدل‌های محلی).

---

## ۰٫۶٫۵ — ادغام شرکت‌کننده‌ها واقعاً روی داده اعمال می‌شود

**اصلاح**
- **ادغامِ ۰٫۶٫۴ فقط روی نمایش بود.** چیپ‌های روی صفحه یکی می‌شدند ولی دادهٔ ذخیره‌شده تکراری می‌ماند — و مهم‌تر، پرامپتِ صورت‌جلسه از روی همان دادهٔ ذخیره‌شده ساخته می‌شود، پس مدل همچنان فهرست تکراری می‌گرفت و نام‌ها را دوباره در سند می‌نوشت.
- حالا نتیجهٔ ادغام ذخیره هم می‌شود: یک بار باز کردنِ جلسه آن را برای همیشه تمیز می‌کند. پیش از ساخت صورت‌جلسه هم بررسی می‌شود تا مدل هیچ‌وقت فهرست تکراری نگیرد.

---

## ۰٫۶٫۴ — یکی‌شدن نام و ایمیل شرکت‌کننده‌ها

**اصلاح**
- **هر نفر دو بار در فهرست شرکت‌کننده‌ها می‌آمد** — یک بار با ایمیل (از تقویم) و یک بار با نام نمایشی (از زیرنویس). حالا این دو به هم چسبانده می‌شوند و نام نمایش داده می‌شود با ایمیل روی نگه‌داشتن نشانگر. در یک جلسهٔ واقعی، فهرست از ۱۷ به ۱۰ رسید.
- ادغام **محتاطانه** است و سه حالت را حفظ می‌کند: کسی که وسط جلسه اضافه شده و در تقویم نبوده (فقط نام)، ایمیلی که نامی برایش پیدا نشد، و دو نفر با نام کوچکِ یکسان — `sara.ahmadi@` به «Sara Karimi» نمی‌چسبد. چسباندن اشتباهِ دو نفر بدتر از دوبار آمدنِ یک نفر است.
- جلسه‌های قبلاً ذخیره‌شده هم موقع خواندن از همین ادغام رد می‌شوند.

**تازه**
- **فیلد «ایمیل شما» در تنظیمات** — تا «You» که Meet در زیرنویس می‌نویسد به خودت وصل شود و به‌عنوان یک نفر جدا نیاید.

---

## ۰٫۶٫۳ — یکی‌شدن اقدام‌ها با بازبینی شواهد

**اصلاح**
- **تبِ «اقدام‌ها» و تبِ «بازبینی شواهد» دو فهرست جدا نشان می‌دادند.** فهرست اقدام‌ها قابل‌ویرایش است ولی بازبینی نسخهٔ منجمدِ `analysisData` را می‌خواند؛ پس بعد از هر ویرایش، حذف یا افزودن، دو تب دو چیز متفاوت می‌گفتند. علتِ ریشه‌ای این بود که `normalizeActions` هنگام ساختِ فهرستِ اقدام‌ها `evidenceRefs` و `confidence` را دور می‌ریخت.
- حالا شواهد و سطح اطمینان روی خودِ اقدام می‌مانند و بازبینی همان فهرستِ زنده را نشان می‌دهد. ارجاع‌های نامعتبر (صفر، منفی، غیرعددی) هم فیلتر می‌شوند.
- اقدامی که خودت اضافه کرده‌ای نشانِ **«دستی»** می‌گیرد — نبودِ شاهد برایش طبیعی است، نه کمبود.
- تحلیل‌های قدیمی از روی ترتیب بازیابی می‌شوند تا شواهدشان از دست نرود.

---

## ۰٫۶٫۲ — رفع چهار ایراد گزارش‌شده در استفادهٔ روزمره

**اصلاح**
- **دکمه‌ها وقتی نشانگر کنارشان بود فعال می‌شدند، نه وقتی رویشان بود.** رگرسیونی از بزرگ‌کردنِ هدف‌های لمسی در ۰٫۲٫۰: ناحیهٔ نامرئیِ ۴۴px برای همه اعمال شده بود، از جمله ماوس. حالا فقط روی صفحهٔ لمسی (`pointer: coarse`) اعمال می‌شود.
- **پاپ‌آپ «تغییر ددلاین» نصفه بیرون از صفحه می‌افتاد.** علتش این بود که اندازهٔ پاپ‌آپ وسطِ انیمیشنِ `scale(.85)` سنجیده می‌شد و عرضش نزدیک صفر خوانده می‌شد، پس محدودسازی به لبهٔ صفحه بی‌اثر بود. حالا با `offsetWidth` سنجیده می‌شود و هر دو محور داخل دید می‌مانند.
- **«زیرکار» در «تمرکز امروز» هیچ کاری نمی‌کرد** و پنل فقط با رندرِ بعدی ظاهر می‌شد. `renderTodoAsync` فقط فهرستِ صفحهٔ «کارها» را می‌سازد، نه فهرست صفحهٔ اول را.
- **ردیف فیلترهای «کارها» در عرض کم صفحه را افقی می‌کشید** — حالا می‌پیچد.

**تازه**
- **انتخاب تاریخ دلخواه برای ددلاین.** میان‌برها (امروز، فردا، آخر هفته…) برای حالت‌های رایج‌اند؛ حالا برای هر تاریخ دیگری یک انتخابگر تاریخ هم هست.
- **«زمان بگذار» به «وقت گذاشتن در برنامهٔ امروز» تغییر نام داد** — معلوم نمی‌کرد چه کار می‌کند (یک بازهٔ خالی از امروز را به آن کار اختصاص می‌دهد).

---

## ۰٫۶٫۱ — درستیِ ارجاع شواهد و نمایش سطح اطمینان

**اصلاح**
- **ارجاع‌های شاهد (T۱، T۲…) در جلسه‌های قبلاً تحلیل‌شده به خط اشتباه می‌رسیدند.** رگرسیونی از شکستنِ نوبت‌های بلند در ۰٫۳٫۰ بود: شمارهٔ خط عوض می‌شد ولی تحلیلِ قدیمی با شماره‌های قبلی ساخته شده بود. روی دادهٔ واقعی **۳۰۵ از ۳۰۷ ارجاع** خراب بودند. حالا شمارهٔ خطِ اصلی روی هر تکه نگه داشته می‌شود و هر دو حالت (تحلیل قدیمی و جدید) درست حل می‌شوند — هر ۳۰۷ ارجاع تأیید شد.

**تازه**
- **سطح اطمینان هر مورد در «بازبینی شواهد» نمایش داده می‌شود** — قطعی / محتمل / مبهم. این داده از قبل استخراج می‌شد ولی هیچ‌جا دیده نمی‌شد. موارد مبهم با نوار کهربایی مشخص می‌شوند تا بدانی کجا باید دقیق‌تر نگاه کنی.

---

## ۰٫۶٫۰ — گزارش جلسه، و رفع افتادنِ نام حاضران

**تازه**
- **قالب «گزارش جلسه (بدون جدول)»** — روایتی و خوانا. پاراگراف به‌جای شبکهٔ سلول‌ها؛ فهرست فقط برای اقدام‌ها. پرامپت صریحاً ساختن هر نوع جدول را ممنوع می‌کند.

**اصلاح**
- **نام بسیاری از حاضران در صورت‌جلسه نمی‌آمد.** دو علت داشت: شمای استخراجِ تکه‌ای هیچ فیلدی برای گوینده ندارد، پس در جلسه‌های بلند (که تکه می‌شوند) نام‌ها پیش از نوشتنِ سند کاملاً از بین می‌رفتند؛ و در حالت تک‌درخواستی هم مدل باید خودش نام‌ها را از متن پیدا می‌کرد. حالا حاضران را خودِ برنامه از تقویم و از نامِ گوینده‌های زیرنویس جمع می‌کند و صریح به مدل می‌دهد. روی یک جلسهٔ واقعیِ پنج‌نفره آزموده شد: هر ۵ گوینده حالا در پرامپت هستند.

---

## ۰٫۵٫۱ — رفع خطای ۴۰۲ روی قالب‌های آزاد

**اصلاح**
- **قالب‌های `freeform` («رسمی» و «خلاصه») کل متن جلسه را در یک درخواست می‌فرستادند.** مسیر ساختاریافته برای متن بلندتر از ۱۴٬۰۰۰ نویسه تکه‌بندی می‌کرد، ولی مسیر آزاد هیچ سقفی نداشت. در دادهٔ واقعی **۱۲ جلسه از ۱۹** از این سقف رد می‌شدند — بزرگ‌ترینشان ۲۰۹٬۲۳۲ نویسه در یک درخواست. سرویس چنین درخواستی را با ۴۰۲ (اتمام اعتبار)، ۴۱۳ (حجم زیاد) یا ۴۲۹ رد می‌کند. حالا متن بلند مثل مسیر ساختاریافته اول تکه‌تکه استخراج می‌شود.
- **پیام‌های خطای سرویس گویا شدند.** پیش‌تر فقط «خطای سرویس (۴۰۲)» بود. حالا هر کد وضعیت می‌گوید مشکل چیست و چه باید کرد — از جمله اینکه ۴۰۲ یعنی اعتبار حساب سرویس تمام شده و ربطی به منشی ندارد.

---

## ۰٫۵٫۰ — واردکردن دستیِ متن جلسه

**تازه**
- **نوشتن/ویرایش متن خام روی صفحهٔ جلسه.** در تب «متن خام» دکمهٔ «نوشتن متن جلسه»؛ هر خط یک نوبت و شکل «نام: متن» گوینده را هم نگه می‌دارد.
- **جلسهٔ تقویمی که ضبط نشده دیگر بن‌بست نیست.** با زدن «صورت‌جلسه ←» روی رویدادی که جلسه‌ای برایش ثبت نشده، فرم جلسهٔ دستی با عنوان، تاریخ، مدت و شرکت‌کننده‌های همان رویداد پر می‌شود.

**اصلاح**
- نوار «متن خام» بازطراحی شد: کنش‌های خود متن از «باز کردن در Claude/ChatGPT/Gemini» جدا شدند. پیش‌تر همه در یک ردیف یکنواخت بودند و بدشکل می‌شکستند.
- دکمه‌های سرویس بیرونی وقتی متنی وجود ندارد غیرفعال‌اند.

---

## ۰٫۴٫۰ — جلسهٔ دستی و قالب رسمی

**تازه**
- **جلسهٔ دستی**: ساخت جلسهٔ حضوری یا تلفنی با عنوان، تاریخ، ساعت، مدت، شرکت‌کننده‌ها و متن. شرکت‌کننده‌ها با شکل `نام <ایمیل>` به پروندهٔ آدم‌ها وصل می‌شوند.
- **ویرایش مشخصات** روی هر جلسه‌ای، حتی ضبط‌شده‌ها. متن ضبط‌شده عمداً در این فرم نمایش داده نمی‌شود تا تصادفی بازنویسی نشود.
- **قالب «صورت‌جلسهٔ رسمی»**: سند قابل‌استناد با شناسنامهٔ سند، حاضر/غایب با عذر، حدنصاب، پیگیری مصوبات جلسهٔ قبل، مصوبه و اقدامِ شناسه‌دار (م‑۱، ا‑۱)، جلسهٔ بعد، «موارد نیازمند تأیید» و بلوک تأیید. قالب پیش‌فرض عوض نشد.

---

## ۰٫۳٫۰ — کاملی ضبط

**اصلاح**
- **خطوط غول‌آسای متن جلسه.** بلوک زیرنویس Meet متن را روی هم انباشته می‌کند و ادغام بی‌کران رشد می‌کرد. در دادهٔ واقعی: خطی با ۱۱٬۹۹۹ نویسه و ۷۵٪ کل متن حبس‌شده در خطوط غول‌آسا. حالا نوبت‌های بلند روی مرز جمله/کلمه شکسته می‌شوند — بدون افت حتی یک کلمه. هنگام خواندن اعمال می‌شود، پس جلسه‌های قبلی هم اصلاح می‌شوند.
- **متن گمراه‌کنندهٔ هشدار.** برچسب «— برای پایان بزن» در حالت هشدار شبیه درخواست پایان جلسه خوانده می‌شد. حالا سکوت («· سکوت») از خرابی جدا شده و متن هشدار مطمئن‌کننده است.
- **از دست رفتن متن هنگام عوض‌کردن پنجره.** مرورگر تایمرها را در تب مخفی کند می‌کند؛ حالا لحظهٔ مخفی‌شدن تب، صف خالی و ذخیره می‌شود (`visibilitychange` و `pagehide`).

---

## ۰٫۲٫۰ — تقویم، صورت‌جلسه و رابط کاربری

**اصلاح — تقویم**
- **جلسهٔ تکراری دوبار نمایش داده می‌شد.** `RECURRENCE-ID` پردازش نمی‌شد، پس هم نمونهٔ `RRULE` و هم نمونهٔ جداگانه ثبت می‌شدند.
- **سری‌های تمام‌شده تا ابد تکرار می‌ساختند** — `COUNT` نادیده گرفته می‌شد.
- **نشست‌های لغوشده نمایش داده می‌شدند** — `STATUS:CANCELLED` روی نمونه بررسی نمی‌شد.
- فهرست جلسه‌ها اصلاً مرتب نمی‌شد؛ و مرتب‌سازی با `startedAt` رشته‌ای بی‌صدا از کار می‌افتاد.

**اصلاح — صورت‌جلسه**
- بخش‌های بی‌محتوا دیگر نوشته نمی‌شوند. استندآپ سه‌خطی از ۴۴۷۱ به ۹۴۵ نویسه رسید؛ «ذکر نشده» از ۲۷ به ۰.
- ستونی که در هیچ سطری مقدار ندارد از جدول حذف می‌شود.
- «مدت» با رقم فارسی نوشته می‌شود.
- «ساخت دوباره» ثانویه شد و اگر سند دستی ویرایش شده باشد تأیید می‌گیرد.

**اصلاح — رابط کاربری**
- کنتراست: از ۲۰ خطا به ۰ در هر دو پوسته. دکمهٔ اصلی در پوستهٔ تیره ۲٫۳۸:۱ بود.
- عدد KPI در پوستهٔ تیره ۱٫۲:۱ بود (کاشی `<button>` رنگ پیش‌فرض مرورگر را می‌گرفت).
- آیتم‌های نمای هفته پس‌زمینهٔ خاکستری پیش‌فرض مرورگر می‌گرفتند.
- فونت زیر ۱۲px: از ۴۴ مورد به ۰.
- هدف‌های لمسی اصلی به ۴۴×۴۴ رسیدند.
- ردیف کار از ۹ کنترل به ۳ کاهش یافت؛ بقیه زیر منوی «⋯».
- تعریف «کار امروز» یکسان شد — پیش‌تر یک صفحه چهار عدد متناقض نشان می‌داد.
- جست‌وجو ارقام فارسی/عربی/لاتین و ی/ك را یکسان می‌بیند.

**تازه**
- **دفتر تصمیم‌ها و ریسک‌ها** — تبی داخل «جلسه‌ها» که تصمیم‌ها و ریسک‌های استخراج‌شده از همهٔ جلسه‌ها را یک‌جا نشان می‌دهد.
- **آمادگی پیش از جلسه** — «از نشست قبلی چه ماند» روی کارت جلسهٔ بعدی.
- **نگاشت مسئول به فرد** — اقدام‌ها به پروندهٔ فرد وصل می‌شوند؛ هم‌نامِ بدون ایمیل عمداً حدس زده نمی‌شود.

---

## ۰٫۱٫۰ — نسخهٔ پایه

ادغام منشی (صورت‌جلسه) و وردست (کارها) در یک اکستنشن.
