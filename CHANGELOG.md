# تاریخچهٔ نسخه‌ها

نسخه در `manifest.json` نگه داشته می‌شود و در «تنظیمات» پایین صفحه دیده می‌شود.
قاعده: هر بستهٔ تغییر، یک شمارهٔ جدید.

---

## ۰٫۹٫۴ — نشانِ تازه، رنگ، سخنِ روز و ساعت مسقط

**نشانِ منشی عوض شد.** تیکِ داخلِ مربع شبیهِ هر اپلیکیشن کارها در دنیا بود. حالا **«م» فارسی** است: سرِ گرد با دنبالهٔ رو به چپ، روی زمینهٔ گرادیانِ سبز. هم هویتِ فارسی دارد، هم در ۱۶ پیکسل خوانا می‌ماند.
آیکن‌های `icon16/32/48/128.png` هم از همان هندسه بازتولید شدند، پس آیکنِ نوارِ مرورگر هم عوض می‌شود — نه فقط داخلِ برنامه.

**رنگ به طراحی اضافه شد — به‌عنوان ساختار، نه تزئین:**
- هر کارتِ کیوسک نوارِ باریکِ رنگی و عنوانِ هم‌رنگِ خودش را دارد: تقویم آبی، اوقات فیروزه‌ای، سخن بنفش، بازار کهربایی، اخبار سبز. چشم سریع‌تر کارتِ موردنظر را پیدا می‌کند.
- نوار «یک‌نگاه» هر سنجه رنگِ خودش را گرفت.
- سربرگِ امروز شست‌وشوی بسیار ملایمِ رنگی گرفت به‌جای زمینهٔ تخت.
- کنشِ اصلی و نشانِ برند همان گرادیانِ هویت را دارند.
دو رنگِ تازه (بنفش و فیروزه‌ای) به پالت اضافه شد، هر دو در پوستهٔ روشن و تیره جداگانه تنظیم شده.

**«بیت روز» شد «سخن روز».** از ۱۴ بیتِ حافظ به **۴۲ سخن از ۲۰ گوینده**:
- شعر: حافظ، سعدی، مولوی، خیام، فردوسی، نظامی، باباطاهر، پروین اعتصامی، شهریار
- نقل‌قول: انیشتین، ماری کوری، ادیسون، ماندلا، جابز، آلن کی، سقراط، لائوتزه، مارکوس اورلیوس، هلن کلر، دراکر
- سه دکمهٔ «همه / شعر / نقل‌قول» و «یکی دیگر» که تضمین می‌شود همان قبلی را ندهد.
- عمداً فقط نقل‌قول‌هایی آمده که انتسابشان محلِ مناقشه نیست؛ جمله‌های پرتکرارِ اینترنتی که به آدم‌های بزرگ نسبت داده می‌شوند ولی سندی ندارند، کنار گذاشته شدند.

**ساعت مسقط** اضافه شد و اول فهرست نشست. نمایشِ هم‌زمانِ چند ساعت از قبل کار می‌کرد ولی راهنمای تنظیمات خلافش را می‌رساند — متنش اصلاح شد: «هر تعداد شهر که خواستی انتخاب کن».

۵ تست تازه برای سخن روز. کنتراست همهٔ عناصرِ رنگی در روشن و تیره سنجیده شد: هیچ موردی زیر حدِ AA نیست.

---

## ۰٫۹٫۳ — چیدمانِ کیوسک

کارت‌ها در یک شبکهٔ ردیفی بودند، پس **بلندترین کارتِ هر ردیف ارتفاعِ کلِ ردیف را تعیین می‌کرد**. «بازار» بلند بود، زیرِ «تقویم» و «بیت روز» حفرهٔ بزرگی می‌ماند و «اخبار» به ته صفحه پرت می‌شد.

- شبکه به **چیدمانِ ستونی** تغییر کرد؛ کارت‌ها پشتِ سرِ هم می‌ریزند و فضای عمودی پر می‌شود. ارتفاعِ صفحه از ۱۷۱۳ به ۱۳۳۵ رسید و حفرهٔ میانی از بین رفت.
- سقفِ دو ستون گذاشته شد تا روی نمایشگرِ بزرگ، کارت‌ها باریک و ناخوانا نشوند. زیر ۷۶۰ پیکسل یک‌ستونه می‌شود.
- هیچ کارتی وسطش بین دو ستون نصف نمی‌شود.
- **شمارش معکوس‌ها دو‌در‌دو شدند.** پیش‌تر سه‌تا کنار هم می‌نشست و چهارمی تنها به خط بعد می‌افتاد.
- **فهرست اخبار به شش خبر کوتاه شد** با دکمهٔ «۱۴ خبر دیگر». هم کارت را از غول‌شدن درآورد، هم به همان قرارِ «این صفحه باید ته داشته باشد» وفادار است.
- ردیف‌های بازار کمی جمع‌تر شدند و توضیحِ سه‌خطیِ پایینش به یک خط رسید (بقیه‌اش روی نگه‌داشتنِ نشانگر).

---

## ۰٫۹٫۲ — بازار، منابع خبر و عکسِ خبر

**کارت بازار** — دلار، یورو، درهم، پوند، لیر · مثقال و طلای ۱۸ عیار · سکهٔ امامی، بهار آزادی، نیم، ربع، گرمی · انس طلا و نقره.

دو تصمیم که ارزش گفتن دارد:

- **هیچ متنی از سایتِ بیرونی وارد رابط نمی‌شود.** نامِ هر قلم از جدولِ خودمان می‌آید و فقط *عدد* از صفحه خوانده می‌شود. تست شده: ردیفی با `<img onerror=…>` در نامش، برچسبِ سالمِ خودمان را می‌گیرد.
- **درصد تغییرِ ارز را خودمان می‌سازیم.** منبع برای ارز درصد نمی‌دهد؛ به‌جای وابسته‌شدن به سرویس دوم، هر بار قیمت ذخیره می‌شود و تغییر از مقایسه با روزِ قبل درمی‌آید. ۳۰ روز نگه داشته می‌شود — کاملاً محلی. برای طلا و سکه، درصدِ خودِ منبع مقدم است.

در دادهٔ واقعی «سکه گرمی: ‎−۹۷٪» دیده شد که آشکارا خرابیِ منبع است؛ درصدِ بالای ۴۰٪ به کاربر نشان داده نمی‌شود.

**منابع خبر از ۲ به ۹ رسید، دسته‌بندی‌شده:**
فناوری (زومیت، دیجیاتو، آی‌تی ایران) · ورزشی (ورزش سه، طرفداری) · اقتصاد (دنیای اقتصاد، اقتصادنیوز) · عمومی (خبرآنلاین، ایسنا).
در کیوسک بالای فهرست، دکمهٔ دسته آمد — و فقط دسته‌هایی که واقعاً خبر دارند دیده می‌شوند. انتخابگرِ تنظیمات هم دسته‌بندی شد، چون فهرستِ تختِ ۹تایی خوانده نمی‌شد.

**عکسِ خبر** — از هر جایی که فید گذاشته باشد: `enclosure`، `media:content`، `media:thumbnail`، `<image>`، یا اولین `<img>` داخل توضیحات. هر سه مسیر تست شد.
امنیت: فقط `https`، و فقط چیزی که واقعاً عکس است. `javascript:` و `data:` رد می‌شوند. عکس‌ها با `referrerpolicy="no-referrer"` بار می‌شوند تا آدرسِ منشی برای سرورِ عکس نرود، و `lazy` هستند. عکسِ خراب خودش را برمی‌دارد و جای خالی نمی‌گذارد.
بندانگشتی ۶۸×۵۰ کنارِ عنوان است، نه بالای آن — فهرست فشرده می‌ماند. کارتِ صفحهٔ «امروز» عمداً بی‌عکس ماند تا آن صفحه آرام بماند.

**تقویم** — «پیروزی انقلاب اسلامی» حذف شد و جایش **«آخر هفته»** آمد: تنها شمارشی که هر روز عوض می‌شود و به برنامه‌ریزی می‌آید. (۶ مرداد سه‌شنبه است → «۲ روز».)

۲۶ تست تازه؛ جمعاً ۲۴۸.

---

## ۰٫۹٫۱ — صفحهٔ «کیوسک»

صفحهٔ مکث، در ریل کنار بقیه. **هرچه اینجاست روی همین دستگاه حساب می‌شود** — هیچ درخواست شبکه‌ای، جز کارت اخبار که اختیاری و پیش‌فرض خاموش است.

**تقویم و مناسبت‌ها** — مناسبت امروز، شمارش معکوس (پایان ماه، پایان فصل، تعطیلی بعدی، نوروز) و پنج مناسبت پیشِ رو با تاریخ. سرریزِ اسفند به فروردین تست شده، پس اواخر سال فهرست خالی نمی‌شود.
عمداً **فقط مناسبت‌های تقویم شمسی**. مناسبت‌های قمری هر سال جابه‌جا می‌شوند و بدون رؤیت هلال دقیق نیستند؛ حدس‌زدنشان بدتر از نگفتنشان بود، و همین را روی خودِ کارت نوشته‌ام.

**اوقات شرعی** — کاملاً محاسباتی، به روشِ مؤسسهٔ ژئوفیزیک دانشگاه تهران (فجر ۱۷٫۷°، مغرب ۴٫۵°، عشا ۱۴°) و بدون ساعت تابستانی. ۱۵ شهر. «تا اذان مغرب ۳ ساعت و ۱۲ دقیقه» بالای کارت.
درستی‌اش با واقعیت سنجیده شد: طول روزِ تهران در ۲۸ ژوئیه ۱۴ ساعت و ۳ دقیقه درمی‌آید — همان عددِ واقعی. عرض‌های قطبی (که خورشید طلوع نمی‌کند) به‌جای NaN، «—» می‌دهند.

**بیت روز** — یک بیت از حافظ، هر روز تازه و در طولِ روز پایدار. «یک فال دیگر» و «کپی».

**اخبار** — همان فید، اینجا به‌شکل فهرستِ کامل (تا ۱۰ خبر با منبع و زمان) به‌جای تیترِ چرخانِ صفحهٔ امروز. کارتِ فشردهٔ «امروز» سرِ جایش ماند.

**هر کارت اختیاری است.** دکمهٔ «چه چیزهایی دیده شود» روی خودِ صفحه — نه سفر به تنظیمات. پیش‌فرض: تقویم و بیت روشن، اوقات و اخبار خاموش.

۲۱ تست تازه. کنتراست همهٔ عناصر در روشن و تیره سنجیده شد: هیچ موردی زیر حدِ AA نیست.

---

## ۰٫۹٫۰ — «کارها» حرفه‌ای شد

پنج تغییر، همه با تست. کارها دیگر فقط فهرستِ کارهای جلسه‌ها نیست.

**۱. روتین‌ها حافظه گرفتند (باگ)**
نمونهٔ بعدیِ یک کار تکرارشونده فقط عنوان و تکرار را می‌برد. یعنی چک‌لیستِ «بستن ماه مالی» هر بار از صفر. حالا برچسب، زیرکارها، توضیحات، سنجاق و تخمینِ زمان همه منتقل می‌شوند — زیرکارها **باز** و با شناسهٔ تازه، چون رخدادِ تازه‌ای است. وقتِ زمان‌بندی‌شده منتقل نمی‌شود (مالِ همان روز بود).

**۲. پیگیری با سن و تلنگر**
هر کارِ سپرده‌شده حالا وضعیت دارد: تازه ← بی‌جواب (۳ روز) ← بی‌خبر (۷ روز) ← از موعد گذشته. ترتیبِ فهرست هم عوض شد: اول آنچه از موعد گذشته، بعد آنچه بیشتر بی‌خبر مانده.
- کپی‌کردنِ «پیش‌نویس یادآوری» **خودش تلنگر را ثبت می‌کند** — کارِ دستیِ اضافه ندارد.
- دکمهٔ زنگوله برای وقتی که بیرون از منشی پیگیری کرده‌ای (زنگ زدی، حضوری گفتی).
- روی نشانِ وضعیت نگه داری: «۱۰ روز از سپردن · ۲ بار تلنگر زده‌ای».

**۳. تخمینِ زمان، وصل به وقتِ آزاد**
از منوی «⋯» بگو کاری چقدر طول می‌کشد (۱۵ دقیقه تا ۳ ساعت). بعد سربرگِ امروز کنارِ «بزرگ‌ترین وقت آزاد» می‌گوید **«۳ کار (۲ ساعت) جا می‌شود»** — و با نگه‌داشتن نشانگر، اسمشان را می‌بینی. انتخاب حریصانه است: فوری‌ترین‌ها اول، و هیچ‌وقت از ظرفیتِ بازه بیرون نمی‌زند.

**۴. مرتب‌سازیِ هوشمند واقعاً هوشمند شد**
قبلاً فقط سطل‌بندیِ تاریخ بود. حالا داخلِ هر سطل امتیازِ فوریت حساب می‌شود: سنجاق، شدتِ عقب‌افتادگی، وقتِ گذاشته‌شدهٔ امروز، کارِ نیمه‌کاره، و بی‌حرکتیِ طولانی. کارِ بی‌تاریخی که ماه‌ها مانده کم‌کم بالا می‌آید — ولی هیچ‌وقت از ددلاینِ امروز بالاتر نمی‌رود.
نشانِ **«۴۵ روز بی‌حرکت»** روی ردیف می‌آید (فقط در فهرستِ کارها، نه در تمرکز امروز).

**۵. مرور کارهای پوسیده**
دکمهٔ «مرور» **فقط وقتی می‌آید که چیزی برای مرور هست**. برای هر کار سه راه: «هنوز لازمه»، تاریخ تازه، یا حذف.
معیار: بیش از ۲۱ روز بی‌حرکت، یا بیش از ۱۴ روز از ددلاین گذشته و ۱۴ روز دست‌نخورده. آخرین کار که رسیدگی شد، دکمه محو می‌شود و به «در جریان» برمی‌گردی.

**در حین ساخت گرفته شد:** «هنوز لازمه» برای کارِ از موعد گذشته هیچ کاری نمی‌کرد — دکمه را می‌زدی و ردیف سرِ جایش می‌ماند، چون معیارِ دیرکرد به دست‌زدن نگاه نمی‌کرد. حالا هر دو مسیر به «تازه بررسی‌اش کرده‌ام» احترام می‌گذارند.

کنتراستِ همهٔ عناصر تازه در پوستهٔ روشن و تیره سنجیده شد (کمینه ۴٫۶ و ۴٫۸).

---

## ۰٫۸٫۵ — نوشتنِ دستیِ صورت‌جلسه

جلسهٔ حضوری متنی ندارد که AI رویش کار کند، و تب «صورت‌جلسه» در آن حالت یک بن‌بست بود: «این جلسه متنی برای تحلیل ندارد» و تمام — هیچ راهی برای نوشتن.

- در هر حالتِ خالی، دکمهٔ **«خودم می‌نویسم»** هست. وقتی جلسه متن ندارد، همین دکمه کنشِ اصلی است.
- به‌جای صفحهٔ سفید، **اسکلتِ آماده** باز می‌شود: عنوان، تاریخ و شرکت‌کننده‌ها از خودِ جلسه پر شده، با سرفصل‌های خلاصه / موضوع‌ها / تصمیم‌ها / اقدام‌ها / جلسهٔ بعد.
- هرچه در جدولِ **اقدام‌ها** بنویسی، موقع ذخیره به تب «اقدام‌ها» هم می‌رود و از آنجا به «کارها» می‌فرستی. ذخیرهٔ دوباره تکراری‌شان نمی‌کند.
- دکمهٔ AI وقتی جلسه متن ندارد **غیرفعال** است و می‌گوید چرا — پیش‌تر می‌شد زد و درخواستِ خالی می‌رفت.
- ویرایشگرِ سند حالا یکی است (چه ویرایشِ سندِ ساخته‌شده، چه نوشتن از صفر) و هر دو `summaryEditedAt` می‌گذارند، پس «ساخت دوباره» قبل از بازنویسیِ دست‌نوشتهٔ تو تأیید می‌گیرد.

---

## ۰٫۸٫۴ — جلسه‌ای که یک نفر یک‌ریز حرف می‌زند

**باگِ اصلی: ضبط بی‌صدا قطع می‌شد.**
بلوکِ زیرنویسِ Meet متن را روی هم انباشته می‌کند. وقتی یک نفر تنها حرف می‌زند — جلسهٔ تک‌نفره، یا دو نفر با یک میکروفون — بلوک هیچ‌وقت عوض نمی‌شود و همان یک «نوبت» بی‌پایان بزرگ می‌شد تا به سقفِ ۱۲٬۰۰۰ نویسه بخورد. از آن لحظه به بعد هر زیرنویسِ تازه **انداخته می‌شد**، بدون هیچ خبری. نتیجه: پیغامِ «۱ خط ثبت شده — زیرنویس بروز نمی‌شود».

بازسازی‌اش کردم: در یک تک‌گویی ۲۱٬۴۸۹ نویسه‌ای، **فقط ۱۱٬۹۸۵ نویسه در ۱ خط** ثبت می‌شد و ۴۴٪ گفتار از بین می‌رفت. با کدِ تازه: **۲۱٬۴۸۹ نویسه در ۱۸ خط، بی‌کم‌وکاست.**

- نوبت که از ۱٬۲۰۰ نویسه گذشت، بسته می‌شود و ادامه در نوبتِ تازه می‌نشیند — به‌جای بزرگ‌شدنِ بی‌کران.
- هر بار فقط **بخشِ تازه** نوشته می‌شود، نه کلِ متنِ انباشته؛ برای همین چیزی دوباره‌نویسی یا تکرار نمی‌شود.
- سقفِ ۱۲٬۰۰۰ به ۲۰۰٬۰۰۰ رفت و دیگر «حدِ گفتار» نیست، فقط محافظِ صفحهٔ خراب است.
- نوبت‌های کوتاه‌تر یعنی ارجاعِ شواهد (T۱، T۲…) هم دوباره معنا دارد.

**پیغام‌ها**
- «زیرنویس بروز نمی‌شود» یک تشخیصِ نادرست بود؛ ما فقط می‌دانیم چیزی نشنیده‌ایم. حالا می‌گوید **«۵ دقیقه چیزی شنیده نشد»** و راه‌حل را هم می‌دهد.
- **«· برای پایان بزن» از کنارِ هشدار برداشته شد.** همان چیزی که در تصویرِ تو دیده می‌شد: کنارِ هشدارِ نارنجی خوانده می‌شد «جلسه دارد تمام می‌شود». حالا فقط در حالتِ عادی می‌آید. (تستِ قبلی این را نمی‌گرفت چون فقط متنِ حالت را می‌دید، نه برچسبِ نهایی — حالا خودِ تابعِ برچسب تست می‌شود.)

---

## ۰٫۸٫۳ — نادیده‌گرفتنِ سرِنخ‌ها و یکدست‌شدن تنظیمات

**«از جلسه‌های اخیر چه ماند»**
- هر ردیف دکمهٔ **×** گرفت. سرِنخ‌ها تلنبار نمی‌شوند: هر چه را نمی‌خواهی می‌بندی و دیگر پیدایش نمی‌شود.
- نادیده‌گرفتن **روی خودِ مورد** ثبت می‌شود، نه روی ردیف. یعنی اگر بعداً اقدامِ تازه‌ای از جلسه‌ای دربیاید، همان ردیف دوباره می‌آید — فقط با موردهای جدید. (تست شد: ۴ اقدامِ نادیده‌گرفته ساکت ماندند، اقدامِ تازه با شمارهٔ ۱ برگشت.)
- بعد از زدن ×، پیامِ **«نادیده گرفته شد» با دکمهٔ واگرد** می‌آید؛ اگر اشتباهی زدی، برمی‌گردد.
- اگر آخرین ردیف هم بسته شود، کلِ کارت جمع می‌شود و جای خالی نمی‌گذارد.
- کلیدهای نادیده‌گرفته پس از **۳۰ روز** خودکار پاک می‌شوند تا فهرست بی‌نهایت بزرگ نشود.

**تنظیمات**
- «سربرگ صفحهٔ امروز» و «میان‌برهای صفحه‌کلید» بیرون از شبکهٔ تنظیمات و بدون قالب رها شده بودند — متن لخت روی زمینه. حالا مثل بقیهٔ بخش‌ها **کارتِ تاشو** هستند: همان کادر، همان پیکان، همان عرض.

---

## ۰٫۸٫۲ — جای کارت اخبار و فاصلهٔ تازه‌سازی

- کارت اخبار **بین سربرگ و بنر پایان روز** نشست (پیش‌تر پایین‌تر بود).
- فاصلهٔ گرفتن فید تازه از **۱۵ به ۱۰ دقیقه** رسید. چرخش تیترها همان هر ۲۰ ثانیه.

---

## ۰٫۸٫۱ — تشخیصِ دقیقِ مشکل فید اخبار

**اصلاح**
- پیام «خبری خوانده نشد» به‌تنهایی نمی‌گفت مشکل کجاست. حالا وضعیتِ **هر منبع جداگانه** گزارش می‌شود و علت را دقیق نام می‌برد: دسترسی داده نشده / سایت پاسخ نداد / خطای HTTP فلان / به‌جای فید صفحهٔ وب برگشت / فید خالی بود.
- **دو دکمهٔ خودسرویس در تنظیمات:** «بررسی اتصال» که هر منبع را همان لحظه امتحان می‌کند و نتیجه را می‌گوید، و «دادن دسترسی به سایت‌ها» که پنجرهٔ اجازهٔ کروم را باز می‌کند.
- پیش از هر درخواست، وجودِ دسترسی با `chrome.permissions.contains` بررسی می‌شود تا به‌جای خطای مبهمِ شبکه، صریح بگوید دسترسی نداری.

---

## ۰٫۸٫۰ — سربرگِ کاربردی‌تر و کارت اخبار

**تازه**
- **«بزرگ‌ترین وقت آزاد» در سربرگ** — طولانی‌ترین بازهٔ خالیِ باقی‌ماندهٔ امروز، مثل «۱۹:۱۵–۲۰:۰۰ · ۴۵ دقیقه». تنها عددی که مستقیم می‌گوید همین حالا چه کاری می‌شود کرد. زیر ۳۰ دقیقه نمایش داده نمی‌شود.
- **ساعتِ شهرها قابل‌تنظیم شد.** پیش‌تر لندن و نیویورک به همه تحمیل می‌شد؛ حالا پیش‌فرض فقط ساعت خودت است و از میان ۱۰ شهر هرکدام را خواستی اضافه می‌کنی.
- **کارت «اخبار فناوری» (اختیاری)** — تیتر تازه از زومیت و دیجیاتو، هر ۲۰ ثانیه یک تیتر، با پیمایش دستی. فید هر ۱۵ دقیقه تازه می‌شود.

**حریم خصوصی**
- کارت اخبار **پیش‌فرض خاموش** است و تنها بخشی از منشی است که به سایت بیرونی وصل می‌شود. هنگام روشن‌کردن، دسترسی به همان دامنه‌ها از خودت پرسیده می‌شود و اگر ندهی، خاموش می‌ماند. فقط عنوان خبرها خوانده می‌شود — هیچ دادهٔ تو ارسال نمی‌شود.
- تیتر خبر با `textContent` درج می‌شود و لینک‌های غیر از `https` دور ریخته می‌شوند، پس فیدِ آلوده نمی‌تواند چیزی اجرا کند.

---

## ۰٫۷٫۱ — بازطراحی آیکن‌ها و کشف‌پذیریِ میان‌برها

**طراحی**
- **کلِ مجموعهٔ آیکن‌ها بازطراحی شد.** پیش‌تر چهار ضخامتِ خط هم‌زمان استفاده می‌شد (۲، ۲٫۲، ۲٫۴، ۳) و هفت اندازهٔ متفاوت — که باعث می‌شد آیکن‌های ریز سنگین‌تر از آیکن‌های درشت به‌نظر برسند. حالا همه‌جا یک ضخامت (۱٫۷۵) و یک مقیاسِ سه‌پله‌ای (۱۴ / ۱۶ / ۲۰) به‌کار می‌رود.
- هندسهٔ چند آیکنِ ضعیف بازکشیده شد: چرخ‌دندهٔ تنظیمات، فهرستِ تیک‌دارِ کارها، آدم‌ها، نمودارِ گزارش، و «سطل».
- **دو آیکنِ گمراه‌کننده عوض شدند:** «پیگیری معطل» به ساعت‌شنیِ خوانا، و «پیشرفت امروز» از شعله به نمودارِ رو به بالا.

**کاربردپذیری**
- **میان‌برهای صفحه‌کلید وجود داشتند ولی هیچ‌جا اعلام نمی‌شدند** — پس عملاً برای کاربر وجود نداشتند. حالا شمارهٔ هر بخش در تولتیپِ ریل می‌آید و فهرست کامل میان‌برها در تنظیمات هست.

---

## ۰٫۷٫۰ — جست‌وجو و پرسش در همهٔ جلسه‌ها

**تازه**
- **تبِ «جست‌وجو و پرسش»** در بخش جلسه‌ها، با یک ورودی و دو حالت:
  - **جست‌وجو** — فوری، کلیدواژه‌ای، بدون ارسال چیزی به بیرون. نتیجه‌ها بر اساس جلسه گروه‌بندی می‌شوند، هر مورد شمارهٔ خط دارد و واژه‌های پیداشده برجسته می‌شوند.
  - **پرسش از هوش مصنوعی** — پاسخ فقط از روی قطعاتِ بازیابی‌شدهٔ همان آرشیو ساخته می‌شود، با ارجاع `[۱]` به هر جلسه‌ای که استفاده شده، و سیر زمانیِ تغییرِ تصمیم‌ها وقتی موضوعی در چند جلسه دنبال شده.
- **منابع زیر پاسخ نمایش داده می‌شوند** — عنوان جلسه، تاریخ، شمارهٔ خط و متن قطعه. منابعی که واقعاً در پاسخ ارجاع شده‌اند برجسته‌اند و بقیه کم‌رنگ («۳ از ۵ قطعهٔ بررسی‌شده»). هر منبع قابل‌کلیک است و همان جلسه را باز می‌کند.
- پرامپت صریحاً حدس‌زدن را ممنوع می‌کند: اگر منابع پاسخ را نداشته باشند می‌گوید «پیدا نشد»، اگر ناقص باشند بخشِ معلوم را می‌دهد و در انتها «آنچه معلوم نشد» را می‌نویسد، و اگر منابع تناقض داشته باشند هر دو را نشان می‌دهد.

**امنیت**
- **آدرس سرویس هوش مصنوعی حالا باید https باشد.** پیش‌تر یک آدرس `http://` پذیرفته می‌شد و در آن حالت کلید API و متنِ کاملِ جلسه رمزنگاری‌نشده روی شبکه می‌رفتند. `localhost` همچنان مجاز است چون از دستگاه بیرون نمی‌رود (برای مدل‌های محلی).

---

## ۰٫۶٫۵ — ادغام شرکت‌کننده‌ها واقعاً روی داده اعمال می‌شود

**اصلاح**
- **ادغامِ ۰٫۶٫۴ فقط روی نمایش بود.** چیپ‌های روی صفحه یکی می‌شدند ولی دادهٔ ذخیره‌شده تکراری می‌ماند — و مهم‌تر، پرامپتِ صورت‌جلسه از روی همان دادهٔ ذخیره‌شده ساخته می‌شود، پس مدل همچنان فهرست تکراری می‌گرفت و نام‌ها را دوباره در سند می‌نوشت.
- حالا نتیجهٔ ادغام ذخیره هم می‌شود: یک بار باز کردنِ جلسه آن را برای همیشه تمیز می‌کند. پیش از ساخت صورت‌جلسه هم بررسی می‌شود تا مدل هیچ‌وقت فهرست تکراری نگیرد.

---

## ۰٫۶٫۴ — یکی‌شدن نام و ایمیل شرکت‌کننده‌ها

**اصلاح**
- **هر نفر دو بار در فهرست شرکت‌کننده‌ها می‌آمد** — یک بار با ایمیل (از تقویم) و یک بار با نام نمایشی (از زیرنویس). حالا این دو به هم چسبانده می‌شوند و نام نمایش داده می‌شود با ایمیل روی نگه‌داشتن نشانگر. در یک جلسهٔ واقعی، فهرست از ۱۷ به ۱۰ رسید.
- ادغام **محتاطانه** است و سه حالت را حفظ می‌کند: کسی که وسط جلسه اضافه شده و در تقویم نبوده (فقط نام)، ایمیلی که نامی برایش پیدا نشد، و دو نفر با نام کوچکِ یکسان — `sara.ahmadi@` به «Sara Karimi» نمی‌چسبد. چسباندن اشتباهِ دو نفر بدتر از دوبار آمدنِ یک نفر است.
- جلسه‌های قبلاً ذخیره‌شده هم موقع خواندن از همین ادغام رد می‌شوند.

**تازه**
- **فیلد «ایمیل شما» در تنظیمات** — تا «You» که Meet در زیرنویس می‌نویسد به خودت وصل شود و به‌عنوان یک نفر جدا نیاید.

---

## ۰٫۶٫۳ — یکی‌شدن اقدام‌ها با بازبینی شواهد

**اصلاح**
- **تبِ «اقدام‌ها» و تبِ «بازبینی شواهد» دو فهرست جدا نشان می‌دادند.** فهرست اقدام‌ها قابل‌ویرایش است ولی بازبینی نسخهٔ منجمدِ `analysisData` را می‌خواند؛ پس بعد از هر ویرایش، حذف یا افزودن، دو تب دو چیز متفاوت می‌گفتند. علتِ ریشه‌ای این بود که `normalizeActions` هنگام ساختِ فهرستِ اقدام‌ها `evidenceRefs` و `confidence` را دور می‌ریخت.
- حالا شواهد و سطح اطمینان روی خودِ اقدام می‌مانند و بازبینی همان فهرستِ زنده را نشان می‌دهد. ارجاع‌های نامعتبر (صفر، منفی، غیرعددی) هم فیلتر می‌شوند.
- اقدامی که خودت اضافه کرده‌ای نشانِ **«دستی»** می‌گیرد — نبودِ شاهد برایش طبیعی است، نه کمبود.
- تحلیل‌های قدیمی از روی ترتیب بازیابی می‌شوند تا شواهدشان از دست نرود.

---

## ۰٫۶٫۲ — رفع چهار ایراد گزارش‌شده در استفادهٔ روزمره

**اصلاح**
- **دکمه‌ها وقتی نشانگر کنارشان بود فعال می‌شدند، نه وقتی رویشان بود.** رگرسیونی از بزرگ‌کردنِ هدف‌های لمسی در ۰٫۲٫۰: ناحیهٔ نامرئیِ ۴۴px برای همه اعمال شده بود، از جمله ماوس. حالا فقط روی صفحهٔ لمسی (`pointer: coarse`) اعمال می‌شود.
- **پاپ‌آپ «تغییر ددلاین» نصفه بیرون از صفحه می‌افتاد.** علتش این بود که اندازهٔ پاپ‌آپ وسطِ انیمیشنِ `scale(.85)` سنجیده می‌شد و عرضش نزدیک صفر خوانده می‌شد، پس محدودسازی به لبهٔ صفحه بی‌اثر بود. حالا با `offsetWidth` سنجیده می‌شود و هر دو محور داخل دید می‌مانند.
- **«زیرکار» در «تمرکز امروز» هیچ کاری نمی‌کرد** و پنل فقط با رندرِ بعدی ظاهر می‌شد. `renderTodoAsync` فقط فهرستِ صفحهٔ «کارها» را می‌سازد، نه فهرست صفحهٔ اول را.
- **ردیف فیلترهای «کارها» در عرض کم صفحه را افقی می‌کشید** — حالا می‌پیچد.

**تازه**
- **انتخاب تاریخ دلخواه برای ددلاین.** میان‌برها (امروز، فردا، آخر هفته…) برای حالت‌های رایج‌اند؛ حالا برای هر تاریخ دیگری یک انتخابگر تاریخ هم هست.
- **«زمان بگذار» به «وقت گذاشتن در برنامهٔ امروز» تغییر نام داد** — معلوم نمی‌کرد چه کار می‌کند (یک بازهٔ خالی از امروز را به آن کار اختصاص می‌دهد).

---

## ۰٫۶٫۱ — درستیِ ارجاع شواهد و نمایش سطح اطمینان

**اصلاح**
- **ارجاع‌های شاهد (T۱، T۲…) در جلسه‌های قبلاً تحلیل‌شده به خط اشتباه می‌رسیدند.** رگرسیونی از شکستنِ نوبت‌های بلند در ۰٫۳٫۰ بود: شمارهٔ خط عوض می‌شد ولی تحلیلِ قدیمی با شماره‌های قبلی ساخته شده بود. روی دادهٔ واقعی **۳۰۵ از ۳۰۷ ارجاع** خراب بودند. حالا شمارهٔ خطِ اصلی روی هر تکه نگه داشته می‌شود و هر دو حالت (تحلیل قدیمی و جدید) درست حل می‌شوند — هر ۳۰۷ ارجاع تأیید شد.

**تازه**
- **سطح اطمینان هر مورد در «بازبینی شواهد» نمایش داده می‌شود** — قطعی / محتمل / مبهم. این داده از قبل استخراج می‌شد ولی هیچ‌جا دیده نمی‌شد. موارد مبهم با نوار کهربایی مشخص می‌شوند تا بدانی کجا باید دقیق‌تر نگاه کنی.

---

## ۰٫۶٫۰ — گزارش جلسه، و رفع افتادنِ نام حاضران

**تازه**
- **قالب «گزارش جلسه (بدون جدول)»** — روایتی و خوانا. پاراگراف به‌جای شبکهٔ سلول‌ها؛ فهرست فقط برای اقدام‌ها. پرامپت صریحاً ساختن هر نوع جدول را ممنوع می‌کند.

**اصلاح**
- **نام بسیاری از حاضران در صورت‌جلسه نمی‌آمد.** دو علت داشت: شمای استخراجِ تکه‌ای هیچ فیلدی برای گوینده ندارد، پس در جلسه‌های بلند (که تکه می‌شوند) نام‌ها پیش از نوشتنِ سند کاملاً از بین می‌رفتند؛ و در حالت تک‌درخواستی هم مدل باید خودش نام‌ها را از متن پیدا می‌کرد. حالا حاضران را خودِ برنامه از تقویم و از نامِ گوینده‌های زیرنویس جمع می‌کند و صریح به مدل می‌دهد. روی یک جلسهٔ واقعیِ پنج‌نفره آزموده شد: هر ۵ گوینده حالا در پرامپت هستند.

---

## ۰٫۵٫۱ — رفع خطای ۴۰۲ روی قالب‌های آزاد

**اصلاح**
- **قالب‌های `freeform` («رسمی» و «خلاصه») کل متن جلسه را در یک درخواست می‌فرستادند.** مسیر ساختاریافته برای متن بلندتر از ۱۴٬۰۰۰ نویسه تکه‌بندی می‌کرد، ولی مسیر آزاد هیچ سقفی نداشت. در دادهٔ واقعی **۱۲ جلسه از ۱۹** از این سقف رد می‌شدند — بزرگ‌ترینشان ۲۰۹٬۲۳۲ نویسه در یک درخواست. سرویس چنین درخواستی را با ۴۰۲ (اتمام اعتبار)، ۴۱۳ (حجم زیاد) یا ۴۲۹ رد می‌کند. حالا متن بلند مثل مسیر ساختاریافته اول تکه‌تکه استخراج می‌شود.
- **پیام‌های خطای سرویس گویا شدند.** پیش‌تر فقط «خطای سرویس (۴۰۲)» بود. حالا هر کد وضعیت می‌گوید مشکل چیست و چه باید کرد — از جمله اینکه ۴۰۲ یعنی اعتبار حساب سرویس تمام شده و ربطی به منشی ندارد.

---

## ۰٫۵٫۰ — واردکردن دستیِ متن جلسه

**تازه**
- **نوشتن/ویرایش متن خام روی صفحهٔ جلسه.** در تب «متن خام» دکمهٔ «نوشتن متن جلسه»؛ هر خط یک نوبت و شکل «نام: متن» گوینده را هم نگه می‌دارد.
- **جلسهٔ تقویمی که ضبط نشده دیگر بن‌بست نیست.** با زدن «صورت‌جلسه ←» روی رویدادی که جلسه‌ای برایش ثبت نشده، فرم جلسهٔ دستی با عنوان، تاریخ، مدت و شرکت‌کننده‌های همان رویداد پر می‌شود.

**اصلاح**
- نوار «متن خام» بازطراحی شد: کنش‌های خود متن از «باز کردن در Claude/ChatGPT/Gemini» جدا شدند. پیش‌تر همه در یک ردیف یکنواخت بودند و بدشکل می‌شکستند.
- دکمه‌های سرویس بیرونی وقتی متنی وجود ندارد غیرفعال‌اند.

---

## ۰٫۴٫۰ — جلسهٔ دستی و قالب رسمی

**تازه**
- **جلسهٔ دستی**: ساخت جلسهٔ حضوری یا تلفنی با عنوان، تاریخ، ساعت، مدت، شرکت‌کننده‌ها و متن. شرکت‌کننده‌ها با شکل `نام <ایمیل>` به پروندهٔ آدم‌ها وصل می‌شوند.
- **ویرایش مشخصات** روی هر جلسه‌ای، حتی ضبط‌شده‌ها. متن ضبط‌شده عمداً در این فرم نمایش داده نمی‌شود تا تصادفی بازنویسی نشود.
- **قالب «صورت‌جلسهٔ رسمی»**: سند قابل‌استناد با شناسنامهٔ سند، حاضر/غایب با عذر، حدنصاب، پیگیری مصوبات جلسهٔ قبل، مصوبه و اقدامِ شناسه‌دار (م‑۱، ا‑۱)، جلسهٔ بعد، «موارد نیازمند تأیید» و بلوک تأیید. قالب پیش‌فرض عوض نشد.

---

## ۰٫۳٫۰ — کاملی ضبط

**اصلاح**
- **خطوط غول‌آسای متن جلسه.** بلوک زیرنویس Meet متن را روی هم انباشته می‌کند و ادغام بی‌کران رشد می‌کرد. در دادهٔ واقعی: خطی با ۱۱٬۹۹۹ نویسه و ۷۵٪ کل متن حبس‌شده در خطوط غول‌آسا. حالا نوبت‌های بلند روی مرز جمله/کلمه شکسته می‌شوند — بدون افت حتی یک کلمه. هنگام خواندن اعمال می‌شود، پس جلسه‌های قبلی هم اصلاح می‌شوند.
- **متن گمراه‌کنندهٔ هشدار.** برچسب «— برای پایان بزن» در حالت هشدار شبیه درخواست پایان جلسه خوانده می‌شد. حالا سکوت («· سکوت») از خرابی جدا شده و متن هشدار مطمئن‌کننده است.
- **از دست رفتن متن هنگام عوض‌کردن پنجره.** مرورگر تایمرها را در تب مخفی کند می‌کند؛ حالا لحظهٔ مخفی‌شدن تب، صف خالی و ذخیره می‌شود (`visibilitychange` و `pagehide`).

---

## ۰٫۲٫۰ — تقویم، صورت‌جلسه و رابط کاربری

**اصلاح — تقویم**
- **جلسهٔ تکراری دوبار نمایش داده می‌شد.** `RECURRENCE-ID` پردازش نمی‌شد، پس هم نمونهٔ `RRULE` و هم نمونهٔ جداگانه ثبت می‌شدند.
- **سری‌های تمام‌شده تا ابد تکرار می‌ساختند** — `COUNT` نادیده گرفته می‌شد.
- **نشست‌های لغوشده نمایش داده می‌شدند** — `STATUS:CANCELLED` روی نمونه بررسی نمی‌شد.
- فهرست جلسه‌ها اصلاً مرتب نمی‌شد؛ و مرتب‌سازی با `startedAt` رشته‌ای بی‌صدا از کار می‌افتاد.

**اصلاح — صورت‌جلسه**
- بخش‌های بی‌محتوا دیگر نوشته نمی‌شوند. استندآپ سه‌خطی از ۴۴۷۱ به ۹۴۵ نویسه رسید؛ «ذکر نشده» از ۲۷ به ۰.
- ستونی که در هیچ سطری مقدار ندارد از جدول حذف می‌شود.
- «مدت» با رقم فارسی نوشته می‌شود.
- «ساخت دوباره» ثانویه شد و اگر سند دستی ویرایش شده باشد تأیید می‌گیرد.

**اصلاح — رابط کاربری**
- کنتراست: از ۲۰ خطا به ۰ در هر دو پوسته. دکمهٔ اصلی در پوستهٔ تیره ۲٫۳۸:۱ بود.
- عدد KPI در پوستهٔ تیره ۱٫۲:۱ بود (کاشی `<button>` رنگ پیش‌فرض مرورگر را می‌گرفت).
- آیتم‌های نمای هفته پس‌زمینهٔ خاکستری پیش‌فرض مرورگر می‌گرفتند.
- فونت زیر ۱۲px: از ۴۴ مورد به ۰.
- هدف‌های لمسی اصلی به ۴۴×۴۴ رسیدند.
- ردیف کار از ۹ کنترل به ۳ کاهش یافت؛ بقیه زیر منوی «⋯».
- تعریف «کار امروز» یکسان شد — پیش‌تر یک صفحه چهار عدد متناقض نشان می‌داد.
- جست‌وجو ارقام فارسی/عربی/لاتین و ی/ك را یکسان می‌بیند.

**تازه**
- **دفتر تصمیم‌ها و ریسک‌ها** — تبی داخل «جلسه‌ها» که تصمیم‌ها و ریسک‌های استخراج‌شده از همهٔ جلسه‌ها را یک‌جا نشان می‌دهد.
- **آمادگی پیش از جلسه** — «از نشست قبلی چه ماند» روی کارت جلسهٔ بعدی.
- **نگاشت مسئول به فرد** — اقدام‌ها به پروندهٔ فرد وصل می‌شوند؛ هم‌نامِ بدون ایمیل عمداً حدس زده نمی‌شود.

---

## ۰٫۱٫۰ — نسخهٔ پایه

ادغام منشی (صورت‌جلسه) و وردست (کارها) در یک اکستنشن.
